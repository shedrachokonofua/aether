# Managed by Ansible: routed Aether site-to-site WireGuard.
net.ipv4.ip_forward = {{ 1 if site_wireguard_enable_forwarding else 0 }}
net.ipv4.conf.all.rp_filter = 2
net.ipv4.conf.default.rp_filter = 2

