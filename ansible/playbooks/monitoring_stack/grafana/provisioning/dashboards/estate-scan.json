{
  "annotations": {
    "list": [
      {
        "builtIn": 1,
        "datasource": { "type": "grafana", "uid": "-- Grafana --" },
        "enable": true,
        "hide": true,
        "iconColor": "rgba(0, 211, 255, 1)",
        "name": "Annotations & Alerts",
        "type": "dashboard"
      }
    ]
  },
  "description": "Estate scanner coverage, inventory diffs, and Nuclei findings (estate_scan ClickHouse).",
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "panels": [
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 0 },
      "id": 1,
      "panels": [],
      "title": "Coverage",
      "type": "row"
    },
    {
      "type": "stat",
      "title": "Successful runs (7d)",
      "id": 2,
      "gridPos": { "h": 4, "w": 4, "x": 0, "y": 1 },
      "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
      "targets": [
        {
          "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
          "rawSql": "SELECT count() AS value FROM estate_scan.scan_runs FINAL WHERE status = 'succeeded' AND coalesce(finished_at, started_at) >= now() - INTERVAL 7 DAY",
          "refId": "A",
          "format": 1
        }
      ],
      "fieldConfig": {
        "defaults": {
          "thresholds": {
            "mode": "absolute",
            "steps": [
              { "color": "red", "value": null },
              { "color": "green", "value": 1 }
            ]
          },
          "color": { "mode": "thresholds" }
        },
        "overrides": []
      },
      "options": {
        "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false },
        "colorMode": "background",
        "graphMode": "none",
        "textMode": "value_and_name"
      }
    },
    {
      "type": "stat",
      "title": "Failed runs (24h)",
      "id": 3,
      "gridPos": { "h": 4, "w": 4, "x": 4, "y": 1 },
      "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
      "targets": [
        {
          "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
          "rawSql": "SELECT count() AS value FROM estate_scan.scan_runs FINAL WHERE status = 'failed' AND coalesce(finished_at, started_at) >= now() - INTERVAL 24 HOUR",
          "refId": "A",
          "format": 1
        }
      ],
      "fieldConfig": {
        "defaults": {
          "thresholds": {
            "mode": "absolute",
            "steps": [
              { "color": "green", "value": null },
              { "color": "red", "value": 1 }
            ]
          },
          "color": { "mode": "thresholds" }
        },
        "overrides": []
      },
      "options": {
        "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false },
        "colorMode": "background",
        "graphMode": "none",
        "textMode": "value_and_name"
      }
    },
    {
      "type": "stat",
      "title": "Hours since last success",
      "id": 4,
      "gridPos": { "h": 4, "w": 4, "x": 8, "y": 1 },
      "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
      "targets": [
        {
          "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
          "rawSql": "SELECT ifNull(dateDiff('hour', max(coalesce(finished_at, started_at)), now()), 999) AS value FROM estate_scan.scan_runs FINAL WHERE status = 'succeeded'",
          "refId": "A",
          "format": 1
        }
      ],
      "fieldConfig": {
        "defaults": {
          "thresholds": {
            "mode": "absolute",
            "steps": [
              { "color": "green", "value": null },
              { "color": "orange", "value": 8 },
              { "color": "red", "value": 24 }
            ]
          },
          "color": { "mode": "thresholds" }
        },
        "overrides": []
      },
      "options": {
        "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false },
        "colorMode": "background",
        "graphMode": "none",
        "textMode": "value_and_name"
      }
    },
    {
      "type": "stat",
      "title": "Actionable findings",
      "id": 5,
      "gridPos": { "h": 4, "w": 4, "x": 12, "y": 1 },
      "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
      "targets": [
        {
          "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
          "rawSql": "SELECT count() AS value FROM (SELECT finding_key, argMax(state, version) AS current_state, argMax(severity, version) AS current_severity FROM estate_scan.findings GROUP BY finding_key) WHERE current_state = 'open' AND current_severity != 'info'",
          "refId": "A",
          "format": 1
        }
      ],
      "fieldConfig": {
        "defaults": {
          "thresholds": {
            "mode": "absolute",
            "steps": [
              { "color": "green", "value": null },
              { "color": "orange", "value": 1 },
              { "color": "red", "value": 5 }
            ]
          },
          "color": { "mode": "thresholds" }
        },
        "overrides": []
      },
      "options": {
        "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false },
        "colorMode": "background",
        "graphMode": "none",
        "textMode": "value_and_name"
      }
    },
    {
      "type": "stat",
      "title": "Services (latest run)",
      "id": 6,
      "gridPos": { "h": 4, "w": 4, "x": 16, "y": 1 },
      "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
      "targets": [
        {
          "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
          "rawSql": "SELECT count() AS value FROM (SELECT * FROM estate_scan.service_observations FINAL) WHERE run_id = (SELECT run_id FROM estate_scan.scan_runs FINAL WHERE status = 'succeeded' ORDER BY coalesce(finished_at, started_at) DESC LIMIT 1)",
          "refId": "A",
          "format": 1
        }
      ],
      "fieldConfig": {
        "defaults": {
          "thresholds": {
            "mode": "absolute",
            "steps": [{ "color": "blue", "value": null }]
          },
          "color": { "mode": "thresholds" }
        },
        "overrides": []
      },
      "options": {
        "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false },
        "colorMode": "background",
        "graphMode": "none",
        "textMode": "value_and_name"
      }
    },
    {
      "type": "stat",
      "title": "Undeclared asset listeners (latest)",
      "id": 7,
      "gridPos": { "h": 4, "w": 4, "x": 20, "y": 1 },
      "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
      "targets": [
        {
          "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
          "rawSql": "SELECT count() AS value FROM (SELECT * FROM estate_scan.service_observations FINAL) WHERE unexpected = 1 AND run_id = (SELECT run_id FROM estate_scan.scan_runs FINAL WHERE status = 'succeeded' ORDER BY coalesce(finished_at, started_at) DESC LIMIT 1)",
          "refId": "A",
          "format": 1
        }
      ],
      "fieldConfig": {
        "defaults": {
          "thresholds": {
            "mode": "absolute",
            "steps": [
              { "color": "green", "value": null },
              { "color": "orange", "value": 1 }
            ]
          },
          "color": { "mode": "thresholds" }
        },
        "overrides": []
      },
      "options": {
        "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false },
        "colorMode": "background",
        "graphMode": "none",
        "textMode": "value_and_name"
      }
    },
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 5 },
      "id": 10,
      "panels": [],
      "title": "Inventory",
      "type": "row"
    },
    {
      "type": "table",
      "title": "Recent scan runs",
      "id": 11,
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 6 },
      "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
      "targets": [
        {
          "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
          "rawSql": "SELECT coalesce(finished_at, started_at) AS time, toString(run_id) AS run_id, profile, status, target_count, probe_count, coverage_ratio, error_code FROM estate_scan.scan_runs FINAL ORDER BY time DESC LIMIT 50",
          "refId": "A",
          "format": 1
        }
      ],
      "options": { "showHeader": true },
      "fieldConfig": { "defaults": {}, "overrides": [] }
    },
    {
      "type": "table",
      "title": "Latest services (inventory)",
      "id": 12,
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 6 },
      "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
      "targets": [
        {
          "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
          "rawSql": "SELECT replaceOne(a.stable_identity, 'ipv4:', '') AS ip, s.port, s.transport, s.product, s.unexpected AS undeclared_asset, s.last_seen_at AS time FROM (SELECT * FROM estate_scan.service_observations FINAL) AS s INNER JOIN (SELECT * FROM estate_scan.assets FINAL) AS a ON s.asset_id = a.asset_id WHERE s.run_id = (SELECT run_id FROM estate_scan.scan_runs FINAL WHERE status = 'succeeded' ORDER BY coalesce(finished_at, started_at) DESC LIMIT 1) ORDER BY ip, s.port LIMIT 200",
          "refId": "A",
          "format": 1
        }
      ],
      "options": { "showHeader": true },
      "fieldConfig": { "defaults": {}, "overrides": [] }
    },
    {
      "type": "stat",
      "title": "Hostname inventory (latest)",
      "id": 13,
      "gridPos": { "h": 4, "w": 4, "x": 0, "y": 14 },
      "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
      "targets": [
        {
          "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
          "rawSql": "SELECT count() AS value FROM estate_scan.inventory_names FINAL WHERE observed_at = (SELECT max(observed_at) FROM estate_scan.inventory_observations)",
          "refId": "A",
          "format": 1
        }
      ],
      "fieldConfig": {
        "defaults": {
          "thresholds": {
            "mode": "absolute",
            "steps": [{ "color": "blue", "value": null }]
          },
          "color": { "mode": "thresholds" }
        },
        "overrides": []
      },
      "options": {
        "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false },
        "colorMode": "background",
        "graphMode": "none",
        "textMode": "value_and_name"
      }
    },
    {
      "type": "stat",
      "title": "CT-only / undeclared",
      "id": 14,
      "gridPos": { "h": 4, "w": 4, "x": 4, "y": 14 },
      "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
      "targets": [
        {
          "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
          "rawSql": "SELECT count() AS value FROM estate_scan.inventory_names FINAL WHERE declared = 0 AND observed_at >= (SELECT max(observed_at) - INTERVAL 1 DAY FROM estate_scan.inventory_observations)",
          "refId": "A",
          "format": 1
        }
      ],
      "fieldConfig": {
        "defaults": {
          "thresholds": {
            "mode": "absolute",
            "steps": [
              { "color": "green", "value": null },
              { "color": "orange", "value": 1 }
            ]
          },
          "color": { "mode": "thresholds" }
        },
        "overrides": []
      },
      "options": {
        "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false },
        "colorMode": "background",
        "graphMode": "none",
        "textMode": "value_and_name"
      }
    },
    {
      "type": "stat",
      "title": "L7-scannable hostnames",
      "id": 15,
      "gridPos": { "h": 4, "w": 4, "x": 8, "y": 14 },
      "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
      "targets": [
        {
          "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
          "rawSql": "SELECT count() AS value FROM estate_scan.inventory_names FINAL WHERE l7_scan_enabled = 1 AND observed_at >= (SELECT max(observed_at) - INTERVAL 1 DAY FROM estate_scan.inventory_observations)",
          "refId": "A",
          "format": 1
        }
      ],
      "fieldConfig": {
        "defaults": {
          "thresholds": {
            "mode": "absolute",
            "steps": [{ "color": "blue", "value": null }]
          },
          "color": { "mode": "thresholds" }
        },
        "overrides": []
      },
      "options": {
        "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false },
        "colorMode": "background",
        "graphMode": "none",
        "textMode": "value_and_name"
      }
    },
    {
      "type": "table",
      "title": "CT-only / undeclared hostnames (report-only)",
      "id": 16,
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 14 },
      "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
      "targets": [
        {
          "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
          "rawSql": "SELECT name, provenance, toString(ipv4) AS ipv4, inventory_revision, observed_at AS time FROM estate_scan.inventory_names FINAL WHERE declared = 0 ORDER BY observed_at DESC, name ASC LIMIT 200",
          "refId": "A",
          "format": 1
        }
      ],
      "options": { "showHeader": true },
      "fieldConfig": { "defaults": {}, "overrides": [] }
    },
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 22 },
      "id": 20,
      "panels": [],
      "title": "Findings",
      "type": "row"
    },
    {
      "type": "table",
      "title": "Actionable findings",
      "id": 21,
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 23 },
      "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
      "targets": [
        {
          "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
          "rawSql": "SELECT min(first_seen_at) AS time, max(last_seen_at) AS last_seen_at, argMax(severity, version) AS current_severity, argMax(template_id, version) AS current_template_id, argMax(matcher, version) AS current_matcher, countIf(state = 'open') AS occurrences, finding_key FROM estate_scan.findings GROUP BY finding_key HAVING argMax(state, version) = 'open' AND argMax(severity, version) != 'info' ORDER BY current_severity ASC, last_seen_at DESC LIMIT 100",
          "refId": "A",
          "format": 1
        }
      ],
      "options": { "showHeader": true },
      "fieldConfig": { "defaults": {}, "overrides": [] }
    }
    ,
    {
      "type": "table",
      "title": "Informational hygiene",
      "id": 22,
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 31 },
      "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
      "targets": [
        {
          "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
          "rawSql": "SELECT min(first_seen_at) AS time, max(last_seen_at) AS last_seen_at, argMax(template_id, version) AS template_id, argMax(matcher, version) AS matcher, countIf(state = 'open') AS occurrences, finding_key FROM estate_scan.findings GROUP BY finding_key HAVING argMax(state, version) = 'open' AND argMax(severity, version) = 'info' ORDER BY occurrences DESC, last_seen_at DESC LIMIT 200",
          "refId": "A",
          "format": 1
        }
      ],
      "options": { "showHeader": true },
      "fieldConfig": { "defaults": {}, "overrides": [] }
    },
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 39 },
      "id": 23,
      "panels": [],
      "title": "Listener changes",
      "type": "row"
    },
    {
      "type": "table",
      "title": "Latest successful run vs previous",
      "id": 24,
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 40 },
      "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
      "targets": [
        {
          "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
          "rawSql": "WITH (SELECT run_id FROM estate_scan.scan_runs FINAL WHERE status = 'succeeded' ORDER BY coalesce(finished_at, started_at) DESC LIMIT 1) AS latest_run, (SELECT run_id FROM estate_scan.scan_runs FINAL WHERE status = 'succeeded' ORDER BY coalesce(finished_at, started_at) DESC LIMIT 1 OFFSET 1) AS previous_run, current_services AS (SELECT replaceOne(a.stable_identity, 'ipv4:', '') AS ip, s.transport, s.port FROM (SELECT * FROM estate_scan.service_observations FINAL) AS s INNER JOIN (SELECT * FROM estate_scan.assets FINAL) AS a ON s.asset_id = a.asset_id WHERE s.run_id = latest_run), previous_services AS (SELECT replaceOne(a.stable_identity, 'ipv4:', '') AS ip, s.transport, s.port FROM (SELECT * FROM estate_scan.service_observations FINAL) AS s INNER JOIN (SELECT * FROM estate_scan.assets FINAL) AS a ON s.asset_id = a.asset_id WHERE s.run_id = previous_run) SELECT * FROM (SELECT c.ip, c.transport, c.port, 'new' AS change FROM current_services AS c LEFT ANTI JOIN previous_services AS p USING (ip, transport, port) UNION ALL SELECT p.ip, p.transport, p.port, 'removed' AS change FROM previous_services AS p LEFT ANTI JOIN current_services AS c USING (ip, transport, port)) ORDER BY change, ip, port",
          "refId": "A",
          "format": 1
        }
      ],
      "options": { "showHeader": true },
      "fieldConfig": { "defaults": {}, "overrides": [] }
    }
  ],
  "schemaVersion": 39,
  "tags": ["security", "estate-scan", "aether"],
  "templating": { "list": [] },
  "time": { "from": "now-7d", "to": "now" },
  "timepicker": {},
  "timezone": "browser",
  "title": "Estate Scan",
  "uid": "estate-scan",
  "version": 1
}
