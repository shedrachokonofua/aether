# Cockpit

This playbook will configure the Cockpit virtual machine. Cockpit is a fedora vm that provides a web-based server management interface for all machines and VMs in the infrastructure via SSH.

## Usage

```bash
task configure:home:cockpit
```
