---
- name: Deploy dnsmasq (split DNS for peer tailnets)
  hosts: home-gateway-stack
  gather_facts: true
  roles:
    - common
  tasks:
    - name: Create dnsmasq config directory
      file:
        path: /etc/dnsmasq
        state: directory
        mode: "0755"
      become: true

    - name: Template dnsmasq config
      template:
        src: ./dnsmasq.conf.j2
        dest: /etc/dnsmasq/dnsmasq.conf
        mode: "0644"
      become: true

    - name: Create dnsmasq pod (rootful)
      containers.podman.podman_pod:
        name: dnsmasq
        state: quadlet
        network: host
        restart_policy: on-failure
        recreate: true
        quadlet_options:
          - |
            [Unit]
            After=tailscale-pod.service

            [Install]
            WantedBy=multi-user.target
      become: true

    - name: Create dnsmasq container (rootful)
      containers.podman.podman_container:
        name: dnsmasq
        image: docker.io/4km3/dnsmasq:latest
        state: quadlet
        pod: dnsmasq.pod
        volumes:
          - /etc/dnsmasq/dnsmasq.conf:/etc/dnsmasq.conf:Z,ro
      become: true

    - name: Stop stale admin dnsmasq quadlet
      ansible.builtin.systemd:
        name: dnsmasq-admin-pod
        state: stopped
        enabled: false
        scope: system
      become: true
      failed_when: false

    - name: Remove stale admin dnsmasq container
      containers.podman.podman_container:
        name: dnsmasq-admin
        state: absent
      become: true

    - name: Remove stale admin dnsmasq pod
      containers.podman.podman_pod:
        name: dnsmasq-admin
        state: absent
      become: true

    - name: Remove stale admin dnsmasq config
      file:
        path: /etc/dnsmasq/dnsmasq-admin.conf
        state: absent
      become: true

    - name: Reload systemd daemon
      ansible.builtin.systemd:
        daemon_reload: yes
        scope: system
      become: true

    - name: Start dnsmasq quadlet
      ansible.builtin.systemd:
        name: dnsmasq-pod
        state: restarted
        scope: system
      become: true
