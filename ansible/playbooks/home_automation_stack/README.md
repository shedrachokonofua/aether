# Home Automation Stack

This playbook is for configuring the home automation stack virtual machine. The home automation stack is a fedora vm that hosts the following applications deployed as podman quadlets:

- Home Assistant

## Usage

```bash
task configure:home:home_automation_stack
```
