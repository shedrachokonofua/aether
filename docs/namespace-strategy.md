# aether-k8s Namespace Strategy

**Date:** 2026-07-02.
**Status:** FINAL. This document **replaces `docs/namespace-strategy.md` (2026-06-21)** in full; the old file was removed from the working tree on 2026-07-02, and this document lands at that same path. The old doc's census facts were re-verified and corrected against the live cluster on 2026-07-02; its decisions (zero renames as conservatism, relabel-in-place-only) are discarded. Do not cite the old doc.
**Evidence:** live read-only censuses of workloads, policy surface, data/secrets, routing, IaC, and out-of-band deployment channels, all dated 2026-07-02 (context `admin@aether-k8s`, verified). Every factual claim below traces to one of those reports or a direct file:line in this repo. Where the 06-21 doc and the live cluster disagree, the cluster won — including the namespace count itself: **the cluster has 54 live namespaces, not 55** (the working list circulated during this design carried 54 names under a 55 header; `kubectl get ns` returns 54).

---

## 1. TL;DR

The cluster's problem is not namespace names. It is that **governance is a function of how a namespace was born, not of what it is.** Seven creation channels (committed tofu, applied-but-uncommitted tofu, three sibling-repo tofus, raw kubectl, helm `create_namespace`, operator side-effects, Kyverno clones) produce seven label postures, and every cross-cutting concern — PSA, netpol, backup, quota, priority, gateway attachment, alerts — is hand-plumbed per app. That is why backups silently rotted twice in five days (affine/immich dumping frozen pre-migration databases while dashboards showed green; mnemo failing ~hourly for 38+ runs), why the previous census missed two 60-day-old colony namespaces, why the password vault shares a secret-visibility domain with six mutable-tag apps, and why nearly every LLM provider key the owner has sits in one 14-app, ~20-pod (49-container) mega-namespace with two autonomous agents and an SSRF-by-design scraper.

The fix, in one sentence: **a namespace contract — 4 required labels (`tier`, `owner`, `backup`, `exposure`) plus a handful of tier-defaulted optionals — from which every runtime behavior is derived mechanically by Kyverno CEL policies + PSA + Cilium + a typed tofu module, enforced identically for namespaces this repo creates and namespaces it never sees.** Unknown namespaces land quarantined (default-deny both directions, zero cloned credentials, self-announcing) instead of open. Names change only where a data migration is already owed; there are a bounded set of moves worth paying for, and each one is executed as a backup-restore, so **the migration program is the restore-drill program**.

End state: ~66–68 small namespaces, one screen, four labels each (`kubectl get ns -L aether.shdr.ch/tier,aether.shdr.ch/backup,aether.shdr.ch/exposure,aether.shdr.ch/owner` tells the whole story). The vault, the LLM keys, the agents, the client site, and the internet's probe traffic each sit in their own blast radius. Pi saturation degrades in a chosen order. A sibling repo can create a namespace on a Saturday and it is governed, fenced, and visible by Sunday.

---

## 2. Design principles

1. **Namespace = blast radius.** It is the scope of secret visibility, PSA, NetworkPolicy, RBAC, quota, and atomic teardown (`delete ns` is the only uninstaller). It is not a folder, a team, or an environment.
2. **Names are immutable identity; labels are mutable attributes.** Names encode only stable function. No env, tier, tool, or arch in a name — ever. Pure renames are pure cost (PV rebinds, DNS literals, S3 key-prefix history forks, metric continuity); renames ride data migrations or don't happen.
3. **One decision per namespace.** Creating a namespace means declaring the contract labels. PSA, netpol, LimitRange/quota, priority, backup plumbing, gateway rights, pull-secret eligibility, arch affinity, and alert severity all derive. A label exists only if a named machine consumes it.
4. **Derivation is implemented once — at admission.** Kyverno derives; the tofu module only declares. Out-of-band creators (colony, osemu, shdrch, node-remediator, GitLab CI, operators) are first-class citizens governed by the same admission path. Convention already failed: the census missed colony-dev for 57 days.
5. **One writer per surface.** Tofu writes Namespaces + contract labels + app-specific allows. Kyverno writes derived objects (`synchronize: true`). Nothing is written by two systems.
6. **Fail-closed on security invariants, fail-open on availability paths.** Unknown namespaces get default-deny and no credentials. Pod-path mutations (priority, arch, kata default) use `failurePolicy: Ignore` so a Kyverno outage can't stop scheduling. PSA and two VAPs run in-process in the apiserver as the engine-independent floor that survives a Kyverno outage — and that holds during waves 0–1 of a rebuild-from-zero, before Kyverno exists.
7. **Backups are a namespace property, checked at admission, proven by restore.** Schedule-green ≠ restore-green: the cluster had green jobs backing up dead databases this week. A `backup=critical` namespace *is* backed up with its own scoped credentials, or admission says why not.
8. **Secret scope is the sharpest splitter.** Any pod in a namespace can mount any Secret in it. If two workloads must not read each other's credentials, they do not share a namespace. This rule dissolves `infra` and `personal` — fully, not "opportunistically."
9. **The relief valve is explicit.** Four PriorityClasses + LimitRanges everywhere make "saturate the Pis" a policy instead of a gamble. Agents and sandboxes evict before Immich or the gateway, never the reverse.
10. **Idle control planes are debt.** 23 knative pods serving zero ksvc are CVE surface and RAM the Pis subsidize. Delete until a consumer exists; reinstalling is one apply.

---

## 3. Taxonomy

### 3.1 Six tiers (`aether.shdr.ch/tier`)

| Tier | Definition | Target members |
|---|---|---|
| **platform** | Cluster machinery: CNI, CSI, PKI, policy engines, operators, observability, GPU enablement, CI runners, remediation. PSA per-ns explicit — privileged only where CSI/CNI/eBPF/hostNet demands it, each carrying a `privileged-justification` annotation. | kube-system, kube-public, kube-node-lease, default (gateway residue, otherwise empty), system, ceph-csi-cephfs, observability (new), gpu-system (new), cert-manager, istio-system, cilium-secrets, kyverno, cnpg-system, crossplane-system, tetragon, trivy-system, policy-reporter, goldilocks, headlamp, gitlab-agent, gitlab-runner, agent-sandbox-system, node-healthcheck-operator-system, aether-k8s-node-remediator, medik8s-leases, wasmcloud-system, ups (new) |
| **app** | A workload the owner or family uses. Baseline PSA ratcheting to restricted; default-deny + explicit allows; backup label mandatory. One namespace per app stack. | affine, immich, nextcloud, matrix, media, jellyfin (new), qbittorrent (new), dawarich, karakeep, miniflux, hoppscotch, temporal, yourspotify, open-design, games, and the ex-pool apps: litellm, openwebui, searxng, firecrawl, ai-serving, mnemo, vaultwarden, composer, orion, vane, memos, bentopdf, snapotter |
| **agent** | Autonomous or arbitrary-code workloads holding **standing credentials**: AI agents, dev workspaces, session controllers. Restricted PSA (kata via exemption where pod specs demand more), **FQDN-allowlisted egress**, no cloned registry credentials, batch priority. | hermes, jupyter, coder, deskplane |
| **sandbox** | Ephemeral untrusted execution. **Never holds standing credentials** — that is the line between agent and sandbox, and it is why they are separate tiers. Kata **required** (mutate + validate-reject), deny-all + DNS + declared tool endpoints, quota + LimitRange, lowest priority, TTL-eligible. | sandboxes, colony-sandboxes-dev |
| **guest** | Workloads owned by another repo or party, running on the shared kernel, deployed by their own IaC/CI. Governed by contract, not trust: quota, egress fence, no cloned creds by default, own alert route (daily digest + backup pages). | colony-dev, osemu-ehis-farms, shdrch, **globalping** |
| **tenant** | A real second party needing its own control plane. Always a vcluster. Host provides quota, egress+ingress fence, scoped backup credentials, host-side snapshots; tenant owns its interior. | vc-seven30 |

A seventh value, **`unclassified`**, is never declared — it is stamped by the quarantine machinery onto any namespace that arrives without a contract (§6.3).

Deliberate tier-set choices: no `ci` tier (gitlab-runner is one namespace; it gets platform tier + a per-ns quota override — a tier of one member is taxonomy for its own sake); no `operator`/`structural` sub-tiers of platform (PSA is per-ns explicit within platform anyway); **coder and deskplane are agent tier now**, not app-with-a-watch-note — coder runs arbitrary-code workspaces (148Gi) with standing creds, and deskplane's SA holds namespace-secrets CRUD plus cluster-wide nodes read; **globalping is guest**, not app — it executes *the internet's* measurement requests with `hostNetwork: true` on the flat LAN, L2-adjacent to Ceph.

### 3.2 What forces a namespace boundary (split when ANY differ)

1. **PSA level** — one privileged pod drags the whole namespace to privileged. Live example ×2: jellyfin's privileged FUSE rclone sidecar AND qbittorrent's gluetun `NET_ADMIN` sidecar (qbittorrent.tf:135-137) jointly keep all 12 media apps privileged. **Both** must leave for media to reach baseline — splitting jellyfin alone does nothing for PSS, because baseline forbids added NET_ADMIN.
2. **Secret visibility** — litellm's provider keys vs. hermes/jupyter/firecrawl; vaultwarden vs. everything.
3. **Egress posture** — FQDN-allowlisted agents can't share a default with open-internet apps.
4. **RBAC principal scope** — an SA needing cluster or ns-wide grants defines its own trust domain (hermes-tungsten, coder, deskplane).
5. **Lifecycle / teardown atomicity** — things that die together live together.
6. **Quota need** — bursty/untrusted gets fenced without taxing neighbors.
7. **Owner repo** — different deployer = different namespace, always.
8. **Conflicting CRDs / operators / untrusted cluster-admin** — not a namespace split; that is the vcluster line.

Do **not** split: tightly-coupled stacks that share secrets and deploy/roll back as one unit (nextcloud+onlyoffice, matrix+bridges, an app + its CNPG + its redis + bootstrap jobs, the *arr stack minus the two privilege-holders). Do not split for versions/variants (labels) or aesthetics.

### 3.3 Deterministic placement rule for any future workload

Apply top-down; first match wins. This goes verbatim into `docs/` and the module README.

```
1. Needs its own API server, conflicting CRDs/operators, or untrusted
   cluster-admin?                                → vcluster: ns vc-<tenant>, tier=tenant
2. Deployed by a repo/party other than aether?   → tier=guest, their name, contract
                                                   labels enforced at admission
3. Ephemeral untrusted code execution, never
   holding standing credentials?                 → tier=sandbox (Sandbox CR in
                                                   `sandboxes`, or ephemeral ns + TTL)
4. Autonomous agent / arbitrary-code workspace
   holding credentials (LLM keys, tokens,
   browser sessions, kubeconfig-adjacent RBAC)?  → tier=agent, its own ns
5. Cluster machinery (storage/network/certs/
   policy/observability/CI/GPU)?                 → tier=platform, upstream default name
6. Otherwise: an app                             → ITS OWN namespace via
                                                   module "namespace", named for its
                                                   stable function. No pool additions,
                                                   ever — infra and personal are being
                                                   deleted, not grandfathered.
```

---

## 4. Naming convention & rename verdicts

### 4.1 Rules (validated at admission for `owner=aether` namespaces)

- **Shape:** flat, lowercase, function-named: `^[a-z]([a-z0-9-]{0,30}[a-z0-9])?$` (RFC 1123 subset, ≤32 chars, no trailing hyphen — keeps CNPG/vcluster synthesized names under limits). **Validated on CREATE only**, with a frozen grandfather list for pre-existing aether-owned names (at minimum `node-healthcheck-operator-system`, which sits exactly at the 32-char cap) — otherwise the Phase 1 relabeling of an existing namespace would trip the very rule this doc ships.
- **Reserved:** `kube-*` (Kubernetes), `vc-*` (vcluster host namespaces), `aether-k8s-*` (in-house cluster controllers). `-system` suffix is reserved for third-party operators keeping their upstream default name (cnpg-system, crossplane-system, …) plus the two new first-party exceptions below — never minted for apps.
- **No env suffixes** (`-dev|-stage|-staging|-prod|-test`) on aether-owned namespaces — this is a single-env cluster; environment belongs at the cluster/vcluster boundary. Guest repos own their names within the label contract (colony-dev's suffix is colony's choice and colony's cost; grandfathered).
- **Function over implementation** for platform subsystems (`observability` not `otel`, `gpu-system` not `nvidia`) so tool swaps are label-cheap. Apps keep app names — an app swap is a new app.

### 4.2 Rename verdicts — the honest ledger

Zero pure renames. Not conservatism — arithmetic. The gap between the live names and the greenfield ideal is labels, machinery, and a bounded set of splits, not names. Costs verified in this repo:

| Verdict | Namespaces | Why |
|---|---|---|
| **Never rename** | `system`, `ceph-csi-cephfs` | Hardest lock in the cluster: the `ceph-rbd`/`cephfs` StorageClass **parameters embed the CSI secret namespaces and SC parameters are immutable**; every one of the ~90 bound ceph PVs carries provisioner/nodeStage secretRefs pointing into them. A rename = new SC + secret-ref surgery on every volume. The grab-bag problem in `system` is solved by **splitting out** observability/GPU (stateless), never by renaming the storage core. |
| **Never rename** | `vc-seven30` | Syncer-encoded service names (`caddy-ingress-x-default-x-seven30`), OTel rewrite regex `^vc-seven30($|-)`, tenant API VIP 10.0.3.21. |
| **Never rename** | `kube-*`, `istio-system`, upstream operator names | Webhooks, chart defaults, community runbooks, hardcoded otel scrape target (`otel_collector.tf:240`). |
| **Never move** | `default/main-gateway` | Load-bearing across three codebases (61 in-repo parentRefs + colony's 4 routes + the vcluster syncer route) + VIP 10.0.3.19 in two Caddyfiles + AdGuard. Governance comes from `allowedRoutes: Selector` + hostname pinning + validation (§5), which requires no move. `default` is emptied of workloads and stays that way. |
| **Dissolve, not rename** | `infra`, `personal` | The names aren't the problem; the secret-visibility domains are. App-by-app extraction; the old names die empty and get deleted. |
| **Split, keep name** | `media`, `system` | media loses jellyfin + qbittorrent (both, §3.2) and drops to baseline; system loses observability + GPU and narrows to storage-CSI-only. |
| **Everything else** | keep | No live name encodes a volatile fact badly enough to pay for surgery. |

Also rejected: the 06-21 doc's forward prefixes (`pool-*`, `app-*`, `sbx-*`) even for new namespaces — prefixes encode tier in the name, violating principle 2; the tier label already carries it and a prefix is one more thing out-of-band creators get wrong.

---

## 5. The namespace contract

**Prefix:** one owned prefix, **`aether.shdr.ch/`** (DNS-owned, matching colony's `colony.shdr.ch/*` pattern; `db_backups.tf` already uses `aether.shdr.ch/component`). The legacy `aether.sh/arm-ok` pod key is grandfathered until the Kyverno CEL rewrite ships, then migrated. Upstream keys (`app.kubernetes.io/*`, `pod-security.kubernetes.io/*`, `istio.io/*`, `goldilocks.fairwinds.com/*`) stay as their owners define them.

### 5.1 Owner-set labels

**Required (the contract — typed variables in the tofu module, validated at admission):**

| Label | Values | Consumed by |
|---|---|---|
| `aether.shdr.ch/tier` | platform \| app \| agent \| sandbox \| guest \| tenant | every derivation in §5.3 |
| `aether.shdr.ch/owner` | aether \| colony \| osemu-ehis-farms \| shdrch \| node-remediator \| seven30 \| … | provenance (the colony-census-miss killer); quarterly attrition sweep; alert annotations |
| `aether.shdr.ch/backup` | critical \| standard \| none | module-provisioned ObjectStore/ScheduledBackup + scoped S3 identity; CNPG admission rule; snapshot schedules; prevent_destroy; delete-guard; freshness alerting |
| `aether.shdr.ch/exposure` | public \| internal \| tunnel \| lan-vip \| none | gateway `allowedRoutes` selectors; HTTPRoute hostname validation; cloudflared gating; LB-service allowance |

**Optional (tier-defaulted by the stamper; declare only to override):**

| Label | Values (default) | Consumed by |
|---|---|---|
| `aether.shdr.ch/egress` | cluster \| **internal** \| internet \| allowlist \| none (tier default) | netpol template selection; toFQDNs CNP for `allowlist`. **`internal` = cluster + home-LAN services; `internet` = world minus RFC1918.** The distinction matters here specifically: hermes has live hairpin dependencies on `*.home.shdr.ch` (LAN Caddy — hermes.tf:15/18/20-22), so `internet` would break it at flip — it needs `internal`-plus-allowlist until each hairpin is converted (§8.2). Tier defaults: app=internal, agent=allowlist, sandbox=none, guest=internal (globalping overrides to internet), tenant=existing CNPs, platform=none unless declared. |
| `aether.shdr.ch/arch` | any \| amd64 \| arm64-preferred (default any) | Kyverno mutate → nodeAffinity (replaces the arch-labeler webhook). `arm64-preferred` = *preferred* Pi affinity so amd64 stays the relief valve. |
| `aether.shdr.ch/criticality` | high \| normal \| low (tier default: platform=high, app/agent/tenant=normal, sandbox/guest=low) | alert severity routing via `kube_namespace_labels` join |
| `aether.shdr.ch/lifecycle` | persistent \| ephemeral (default persistent) | ephemeral ⇒ TTL required + CleanupPolicy; persistent+critical ⇒ delete-guard (§5.3 row 14) |
| `aether.shdr.ch/registry-access` | dockerhub \| **none** (default none) | gates the Docker Hub pull-secret clone and imagePullSecrets mutation. `dockerhub` is an explicit allowlist, not a tier default: the 2026-07-02 live-image census plus known on-demand session image use requires it for affine, coder, dawarich, deskplane, globalping, hoppscotch, immich, infra (hermes shared namespace), karakeep, matrix, media, miniflux, nextcloud, osemu-ehis-farms, personal, system, temporal, vc-seven30, and yourspotify. Mirroring images to the GitLab registry retires each override. Structural namespaces without Docker Hub pulls are stamped `registry-access=none`. |
| `aether.shdr.ch/runtime` | runc \| kata (default: runc; sandbox tier forced kata) | RuntimeClass mutate + validate |
| `aether.shdr.ch/mesh` | ambient \| off (default off) | passthrough to `istio.io/dataplane-mode`. **Not derived from tier**: ambient covers 2/54 namespaces today — deriving mesh would imply an enforcement layer that doesn't exist. Revisit when rollout resumes. |

**Derived (not app-owned):** the tofu namespace module now writes the signal-only PSA defaults `pod-security.kubernetes.io/{warn,audit}=restricted` for managed namespaces while preserving explicit overrides; Kyverno still fail-open stamps missing PSA/contract defaults for out-of-band namespaces. Other derived labels remain Kyverno-owned: `pod-security.kubernetes.io/enforce`, `goldilocks.fairwinds.com/enabled`, `istio.io/dataplane-mode`, and `aether.shdr.ch/gateway-access`.

**Escape hatch (rare, audited):** `aether.shdr.ch/pss-override: privileged|baseline` — honored only when a VAP confirms `tier=platform` or a frozen grandfather list; every use is a standing PolicyReport entry. Steady-state target ≤ 9 (CSI ×2, istio-cni, tetragon, kepler→observability, trivy, gitlab-runner, games, globalping), plus a **dated transitional list, distinct from the steady-state target**: media until the Phase 5 split.

**Annotations (prose, never selectors):** `aether.shdr.ch/description`; `aether.shdr.ch/source` (tf file or sibling repo URL); `aether.shdr.ch/hostnames` (comma-separated allowlist — every HTTPRoute host in the ns must be ⊆ it); `aether.shdr.ch/privileged-justification` (mandatory with pss-override); `aether.shdr.ch/ttl` (mandatory iff ephemeral); `aether.shdr.ch/data=rebuildable` (declares why backup=none is fine for a PVC-holding ns).

Rejected from the schema: a separate `data-class` label (zero-trust carried both tier and data-class feeding only alert routing — two labels doing overlapping work is a tired-operator mis-pick trap; `criticality` covers it) and a hand-set `restore-wave` label (gitops-sre; its own tier table showed the mapping is ~bijective — restore order **derives from tier** + the tofu DAG, §5.3 row 15 / §8.13).

### 5.2 The tofu module (creation-side typing)

```hcl
module "namespace" {
  source      = "../modules/namespace"
  name        = "immich"
  tier        = "app"            # enum-validated at plan time
  backup      = "critical"
  exposure    = "internal"
  hostnames   = ["immich.home.shdr.ch"]
  description = "Photo library"
  source_file = "tofu/home/kubernetes/immich.tf"
  # optional: egress, arch, criticality, lifecycle, registry_access, runtime, mesh
}
```

The target module contract is: (a) the Namespace with contract labels + annotations plus module-owned signal-only PSA defaults for `warn`/`audit` where the namespace does not explicitly override them; (b) when `backup != "none"`, the **per-namespace SeaweedFS S3 identity** (scoped to `s3://aether-db-dumps/<ns>/*`), its credentials Secret, and the CNPG-I Barman-Cloud `ObjectStore` + `ScheduledBackup` CRs; (c) `prevent_destroy` on the namespace's stateful resources when `backup=critical`; (d) nothing else — netpol/LimitRange/quota baselines belong to Kyverno so out-of-band namespaces get identical treatment. **Applied checkpoint 2026-07-02:** the module now owns namespace contract labels/annotations, adoption mechanics, and managed-namespace PSA warn/audit signal labels; the per-namespace SeaweedFS identity, CNPG-I backup objects, and structural `prevent_destroy` guard for `backup=critical` remain Phase 3 work. **Adoption mechanics, two classes:** the 33 existing `kubernetes_namespace_v1` resources adopt the module via `moved` blocks (state-only); the 7 Helm-owned namespaces are imported and their releases use `create_namespace=false`; all 54 live namespaces have the four contract labels.

### 5.3 Derivation matrix — exactly what each label buys, via what mechanism

| # | Behavior | From | Mechanism | Detail |
|---|---|---|---|---|
| 1 | **PSA** | tier (+ pss-override) | tofu namespace module writes warn/audit signal labels for managed namespaces; Kyverno MutatingPolicy stamps missing ns labels for out-of-band namespaces; apiserver enforces in-process. Talos cluster default (enforce=baseline — empirically confirmed live via rejections in unlabeled `default`/`personal`) stays as floor; warn/audit=restricted is the Phase 2 signal layer | platform: per-ns explicit + justification · app/guest: enforce=baseline, warn+audit=restricted, per-ns ratchet · agent/sandbox: enforce=restricted (+ kata exemption §8.3) · tenant: baseline |
| 2 | **NetworkPolicy baseline** | tier + egress | Kyverno GeneratingPolicy, `synchronize: true`, generate-existing. **Two Cilium-specific facts shape the template.** (1) Cilium implements the Gateway (cilium.tf:53) with kube-proxy replacement: gateway-proxied traffic reaches backends with Cilium's reserved `ingress` identity, not a pod identity in `default` — a plain NetworkPolicy `namespaceSelector` allow for the gateway ns **matches nothing**, and the first per-ns deny flip would cut all gateway→app traffic. The generated ingress baseline is therefore a **CiliumNetworkPolicy with `fromEntities: [ingress]`**, verified against a canary ns with Hubble before any flip. (2) One **CiliumClusterwideNetworkPolicy baseline** (allow kube-dns :53 + apiserver/health cluster-wide) ships before any per-ns deny flip so per-ns deny composes safely without every template re-carrying DNS allows | app: deny-ingress-except {gateway entity, same-ns, scrapers}; egress per value (internal/internet/cluster). **Scraper allowance is phase-aware: the live scrapers (otel collector, node-exporter, ksm) sit in `system` until the Phase 5 observability move — the generated allow targets `system` until then and switches to `observability` in the same commit as that move**, otherwise every app flipped to default-deny in Phase 2 silently loses metrics/logs for months · agent: deny-all + DNS + tofu-owned CiliumNetworkPolicy toFQDNs allowlist · sandbox: deny-all + DNS + declared tool gateway (ns-wide, closing colony-sandboxes-dev's label-conditional hole) · guest: deny-ingress-except-gateway + egress per value; globalping gets `toEntities: world`, deny RFC1918 · tenant: keeps its hand-built egress CNPs (tofu-owned) **plus a new ingress CNP — vc-seven30's live isolation is egress-only; cluster-lateral movement into the tenant is open today** · platform: exempt initially, staged last. App-specific allows (hermes→media, apps→litellm) stay tofu-owned next to the app |
| 3 | **Mesh** | mesh label | stamper syncs `istio.io/dataplane-mode` | off by default; deliberate per-ns opt-in until ambient is real |
| 4 | **LimitRange + quota** | tier | Kyverno GeneratingPolicy per-tier profiles | LimitRange **defaultRequest only** (100m/256Mi) on **every** app/agent/sandbox/guest/tenant ns — no default CPU/memory limits, because injecting a 500m/512Mi limit into pods that already request more rejects valid GPU/AI workloads (confirmed against infra on 2026-07-03); ResourceQuota on sandbox (current `sandboxes` values), tenant (vc-seven30: first ever — start 32 CPU/64Gi/250Gi; today it is unbounded at 163Gi), guest (modest), + per-ns override for gitlab-runner |
| 5 | **Backup** | backup | tofu module provisions; CNPG executes; **Kyverno ValidatingPolicy enforces** | critical: continuous WAL + daily base via Barman-Cloud CNPG-I plugin, 30d retention, drill pool, prevent_destroy, delete-guard · standard: WAL + daily base, 14d · none + PVCs: audit unless `data=rebuildable` · **Enforce: a CNPG Cluster in a backup≠none ns without a backup section is rejected at admission** — the mnemo bug becomes impossible · non-CNPG state: CSI snapshots (§8.8) + dump CronJobs (mongo/mysql/postgis) keyed to the label with scoped creds |
| 6 | **Goldilocks** | tier | stamper syncs the label | app/agent/guest: true · platform/sandbox/tenant: false. Recommendation-only; VPA auto-mode never |
| 7 | **Alert severity** | criticality (tier-defaulted) | ksm `--metric-labels-allowlist=namespaces=[aether.shdr.ch/tier,aether.shdr.ch/owner,aether.shdr.ch/backup,aether.shdr.ch/criticality]` (explicit keys — ksm's allowlist does not support prefix globs) → PromQL join on `kube_namespace_labels` → 4 Alertmanager routes; same keys into OTel `k8sattributes` | high: page at warning · normal: page at critical · low: daily digest (ends shdrch's 3-consecutive-silent-cron-failures) · backup-freshness alerts page regardless of tier · plus the structural PSS-blocked alert (§8.1) pages everywhere |
| 8 | **Gateway attachment** | exposure | Three layers: (a) every `main-gateway` listener flips `allowedRoutes.namespaces.from: All → Selector` on the stamped `gateway-access` label; (b) **listener hostnames stay pinned** (they already are — keep it a stated invariant); (c) Kyverno validate: HTTPRoute hostnames ⊆ ns `hostnames` annotation, Audit→Enforce | public: public-hostname listeners (tv, nextcloud-public, seven30×2) · internal: `*.home.shdr.ch` · **tunnel: no gateway; a Kyverno validate rule permits cloudflared images ONLY in tunnel-labeled namespaces** — osemu's second ingress path becomes a declared, governed door · lan-vip (games): no gateway, LB Service allowed · none: HTTPRoute creation rejected |
| 9 | **Pull-secret cloning** | registry-access | rewrite `dockerhub-pull-secret` as a label-gated GeneratingPolicy — **and port the policy's two other cluster-wide rules (kyverno.tf:183-217, mutating imagePullSecrets onto every ServiceAccount and Pod) in the same change**, gated on the same label, or pods in gated namespaces reference a nonexistent secret | ends clone-to-every-namespace (today: `dockerhub-creds` is present in **all 54 namespaces** — the generate rule excludes only kyverno, which holds the tofu-created source secret — including kube-public, sandboxes, colony-sandboxes-dev, vc-seven30). Agent/sandbox/guest/tenant/**unclassified** get nothing by default (explicit overrides: §5.1). **Kyverno does not garbage-collect already-generated resources when a rule narrows: a one-time cleanup step deletes the orphaned `dockerhub-creds` Secrets from gated namespaces** |
| 10 | **Priority class** | tier | 4 PriorityClasses (tofu): `aether-platform` (100000) > `aether-app` (1000) > `aether-batch` (100) > `aether-sandbox` (0, `preemptionPolicy: Never`, **`globalDefault`**); Kyverno pod mutate by ns tier, failurePolicy=Ignore; **plus a narrow fail-closed ValidatingPolicy: a pod may only use the PriorityClass its namespace tier maps to, or lower** — without it, any pod spec in a guest/sandbox/agent ns could declare `priorityClassName: aether-platform` and preempt apps, making the whole ladder advisory | platform → aether-platform · app/tenant → aether-app · agent/guest → aether-batch · sandbox → aether-sandbox. **The LOWEST class is the globalDefault, deliberately: the mutate is fail-open (Ignore) by design (§2.6), so a Kyverno outage must degrade an app's priority, never elevate a sandbox's** — and a low default avoids the transition hazard where a mid-priority globalDefault makes every new pod outrank every existing (priority-0) pod cluster-wide, preempting CSI/CNPG/Kyverno itself on the saturated Pis. Rollout: classes first → mutate soak → rolling restart of platform then app tiers so long-lived pods pick up their class. This is what makes Pi saturation safe |
| 11 | **Arch scheduling** | arch | Kyverno pod mutate (nodeAffinity append, Ignore); **retires the in-house arch-labeler webhook** | amd64: required affinity · arm64-preferred: preferred Pi weight · any: untouched. The ARM guardrails (requests set, ≤512Mi) survive as a CEL ValidatingPolicy with **tier-selector exclusions instead of the rotting hardcoded lists** |
| 12 | **Lifecycle/TTL** | lifecycle (+ttl) | Kyverno CleanupPolicy by ns age (avoiding the ns-TTL-label bug kyverno#9053) | ephemeral namespaces stop accreting unnoticed |
| 13 | **RuntimeClass** | tier=sandbox forced; agent opt-in via runtime=kata | Kyverno mutate default + validate-reject non-kata in sandbox tier (Enforce) | the `kata` RuntimeClass is platform-owned in `runtime_classes.tf`. Note: kata and nvidia runtimeClasses are mutually exclusive — GPU workloads (jupyter) therefore live in **agent**, never sandbox |
| 14 | **Namespace DELETE guard** | lifecycle + backup | Kyverno ValidatingPolicy on Namespace DELETE | deleting a persistent + backup=critical namespace is blocked unless `aether.shdr.ch/allow-delete=true` is set in the same change — fat-finger insurance for vaultwarden/immich at N=1 |
| 15 | **Restore order** | tier (derived, rare per-ns override annotation) | tofu module DAG + `task dr:plan` renders live label state into an ordered rebuild checklist | platform storage/PKI → policy → CNPG operator → observability → apps by backup tier → agents/guests/tenant. Declared, not remembered; derived, so it can't rot (§8.13) |

### 5.4 The database sub-contract (CNPG)

All 12 clusters gained backup on 2026-07-02 via the **in-tree `barmanObjectStore`** (`cnpg_backups.tf`) — the right triage call, but it converts the plugin question from "adopt early, never migrate" into a **known debt: in-tree Barman is deprecated in CNPG 1.26 and removed in 1.30, the cluster runs 1.29.1, so the migration to the Barman Cloud CNPG-I plugin is owed in Phase 3, before any operator upgrade past 1.29**. Target shape stays: one `ObjectStore` per namespace, module-provisioned, per-ns credentials, `serverName` = cluster name — history keys to the app, not hand-typed prefixes, and survives any future move via `bootstrap.recovery`. (The interim config already keys its destination to the cluster name — `s3://aether-db-dumps/cnpg/<cluster>` — but runs on the shared admin credential, §8.4.) SeaweedFS hardening in the same stroke: bucket **versioning + object-lock + lifecycle** expiry a few days past Barman retention (today: single admin identity, no lifecycle, unbounded growth, and the owner's own backup history includes a "self-delete defused" incident). Keep `cnpg-require-ceph-rbd-storage` (ported to CEL). CSI snapshots **exclude CNPG PVCs** — snapshot-without-fencing is unsafe; Postgres restores only via `bootstrap.recovery`. And the **dual-DB-window rule**: the CNPG-backup admission rule at Enforce PLUS "legacy postgres STS deleted within 7 days of verified cutover" — the affine/immich silent-data-loss class was a dual-DB-window bug. All four remaining cutovers (litellm, openwebui, matrix, nextcloud) landed **in place** on 2026-07-02; their legacy STSes sit at 0 replicas with PVCs retained, which keeps a dual-DB window open until each retirement — the rule governs those retirements and every future namespace move.

---

## 6. Enforcement architecture

### 6.1 Mechanism split

| Layer | Enforces | Failure mode | Covers out-of-band ns? |
|---|---|---|---|
| **tofu `modules/namespace`** | The authoring interface: typed enums, plan-time errors, backup plumbing, moved-block adoption | n/a (plan-time) | ❌ by definition — which is why every row below exists |
| **PSA (in-apiserver)** | Pod security floor. Cluster default enforce=baseline stays; warn/audit=restricted added cluster-wide; per-ns labels derived from tier; **static exemption `runtimeClassNames: [kata]`** via Talos AdmissionConfiguration | in-process; survives everything | ✅ |
| **VAP ×2 (in-apiserver CEL, GA)** | Engine-independent invariants that must hold while Kyverno is down **and during waves 0–1 of a rebuild-from-zero before Kyverno exists**: (a) Namespace with `enforce=privileged` requires tier=platform or the grandfather lists; (b) Namespace CREATE without `aether.shdr.ch/tier` by non-exempt principals is denied (exempt: system controllers, vcluster syncer). **Rollout: (b) ships with `validationActions: [Audit]` in Phase 1 and flips to Deny only when require-labels flips in Phase 2, after the sibling label PRs merge** — otherwise colony/osemu/shdrch namespace CREATEs are apiserver-denied from week 1 while §6.4.3's 30-day grace is still running | in-process; fail-closed on high-severity invariants, Audit first where adoption grace is promised | ✅ |
| **Kyverno — CEL v1 types only** (1.17 promoted them; legacy ClusterPolicy is deprecated — the 5 existing legacy policies are ported in the same PR, so the contract isn't built on an API with a removal date) | Everything stateful: mutate-stamp + quarantine, generate-with-synchronize (netpol/LimitRange/quota/gated pull-secret), validate (label schema, HTTPRoute hostnames, CNPG backup, sandbox runtime, PriorityClass legality, delete-guard, cloudflared gate, digest-pin, CRB audit) | Namespace/CNPG-Cluster/HTTPRoute validations: Enforce, fail-closed (low-volume kinds). Pod mutations: fail-open (Ignore) — an engine outage degrades hygiene, never availability. **require-labels enforces on CREATE and label-UPDATE only — it never blocks a running namespace** | ✅ |
| **Gateway API `allowedRoutes: Selector`** + pinned listener hostnames + hostname-allowlist validation | route attachment = a deliberate namespace property | data-plane native (Cilium); unlabeled ns simply can't attach | ✅ |
| **Cilium** | generated CNP baselines natively (`fromEntities: [ingress]` for gateway traffic, §5.3 row 2); **one CCNP cluster baseline (allow kube-dns :53 + apiserver/health) deployed before any per-ns deny flip**; toFQDNs CNPs for the agent tier; Hubble = per-ns rollout evidence AND the compensating telemetry for kata pods (host eBPF/Tetragon is blind inside the VM — the netpol IS the sensor for the most autonomous workloads) | staged fail-open → per-ns deny | ✅ |

**Every hardcoded namespace exclusion array dies in the CEL port.** The 3 Kyverno policies and the arch-labeler webhook selector each carry hand-maintained lists that already silently omit the 9 post-census namespaces (7 new since 06-21 + the 2 census-missed colony namespaces); they become `namespaceSelector: tier in (platform)` — self-maintaining forever.

### 6.2 Rollout discipline

Every validate policy ships Audit → (fix findings, ≥1 week clean PolicyReports) → Enforce, per policy. Netpol per namespace: generate with observation first, flip default-deny per-ns with Hubble evidence. PSA: warn/audit=restricted everywhere immediately (pure signal); enforce ratchets per-ns only after missing-pod alerting is live. The 2026-07-02 checkpoint shipped the broad ReplicaSet-not-ready guard (§8.1); the sharper PSS/FailedCreate-specific join should land before any new PSA enforce flip, because enforce evaluates Pods, not Deployments, and this cluster has two live instances of silent 0/1 rejection.

**Applied checkpoint 2026-07-04:** the sharper PSA rejection alert is live — Grafana Loki rule `k8s-pod-rejected-psa` fires on Kubernetes `FailedCreate` events that `violates PodSecurity` (plus `k8s-pod-create-blocked` for pods stuck pre-run and `k8s-namespace-unclassified` for the quarantine tier). This satisfies the enforce-flip gate: a per-namespace PSA `enforce` ratchet may now proceed, because silent 0/1 rejections surface within 15m instead of sitting for days. Also implemented this change (§5.3 row 7 / §8.12): kube-state-metrics runs `--metric-labels-allowlist=namespaces=[aether.shdr.ch/tier,…owner,…backup,…criticality]`, the `aether:namespace_contract:info` recording rule joins those labels into namespace-scoped Prometheus rules, and the four criticality/severity Alertmanager routes (`channel`, `domain`, `criticality=low|high`, `severity`) are provisioned in `notification-policies.yml`.

### 6.3 The quarantine (`tier=unclassified`) — census blindness becomes an alert

Any namespace that arrives without contract labels (a new sibling repo, a helm chart, an operator) is mutate-stamped `tier=unclassified` and generated: PSA baseline + **default-deny both directions** + **zero cloned credentials** + `aether-sandbox` priority + a standing PolicyReport violation + a weekly live-vs-declared diff (module list + known-external registry) posted to ntfy. Quarantined namespaces **run** (nothing breaks at 3am) but cannot reach the network, the gateway, or any shared credential until classified. colony-dev sat invisible for 57 days; the next stranger self-announces in minutes and is fenced while it waits. Deliberately NOT the guest tier: an unattributed namespace must never inherit guest defaults (guest can be granted registry access; unclassified gets nothing, ever).

**How quarantine reconciles with the CREATE gates (§6.1 VAP b, §6.4.3 Enforce):** mutating admission runs before validation, so with Kyverno healthy an unlabeled namespace is stamped `tier=unclassified` *first* and **`unclassified` is a passing value for both the VAP and require-labels** — the namespace is admitted, quarantined, and self-announcing. With Kyverno down, the stamper never fires and the VAP fail-closes: CREATE is denied. Degraded mode is deny; normal mode is quarantine — neither gate is dead code and neither is vacuous. A creator self-declaring `tier=unclassified` is harmless: it is self-fencing. **Sequencing guard: quarantine's *generate* rules (the default-deny fence) have no Audit mode — they activate only after all 54 live namespaces carry contract labels (end of Phase 1)**, so the fence can never land on a not-yet-labeled infra or personal mid-migration; the mutate stamp and the PolicyReport/ntfy announcement are live from day one.

### 6.4 Governing the seven deployment channels

1. **This repo, committed tofu** — module-typed. CI fails if `kubernetes_namespace_v1` appears outside the module or any `helm_release` sets `create_namespace=true`.
2. **This repo, out-of-band-of-committed-tofu — two sub-channels, and the second is the dangerous one.** (a) *Applied-but-uncommitted tf files* (holyclaude.tf, orion.tf, db_backups.tf until 07-02, when they were committed mid-program; a rolling set of modified files remains) — banned as a standing state: Phase 0 commits the remainder; nightly CI `tofu plan -detailed-exitcode` + working-tree drift alert catches divergence within 24h. (b) ***kubectl-applied resources with no tf at all*** — this was the largest live drift class: affine, immich, litellm, openwebui, matrix, and nextcloud CNPG clusters existed only in live/state, and the repo would have rolled some apps back to legacy DB hosts. Reconciled 2026-07-02 by declaring the adopted clusters/secrets in tofu and retargeting the affected app and backup configs to the CNPG writer services. The nightly `tofu plan` job is still the standing detector for both sub-channels. The cluster's source of truth must not be one laptop's working tree — or one session's kubectl.
3. **Sibling-repo tofu** (colony, osemu-ehis-farms, shdrch) — governed at admission: stamper fills what they omit, quarantine fences what it can't classify. Follow-up: a ~10-line PR to each sibling adding the 4 required labels (colony already labels well; it needs only `tier` + `owner`; its `colony.shdr.ch/*` keys stay its own business). The contract interface is `docs/namespace-contract.md`. 30-day grace (VAP b and require-labels in Audit throughout), then Enforce on CREATE — flipped only after the sibling PRs merge (§6.1).
4. **Sibling repo via raw kubectl** (aether-k8s-node-remediator) — same admission coverage; resolve the split-brain by **adopting** the ns + deployment into this repo (it already owns the RBAC half via `node_remediation.tf:27`).
5. **Operator side-effects** (medik8s-leases, cilium-secrets) — exempt list, tier=platform, stamped by mutate.
6. **vcluster interior** — invisible by design; the host contract applies to `vc-seven30` itself (quota, egress+ingress CNPs, snapshots, offered backup creds).
7. **Kyverno-generated** — the generator is the contract.

**The residual hole is CI's god-token, and it makes every admission control advisory until fixed.** `gitlab-agent/gitlab-agent` and `headlamp/headlamp` hold cluster-admin CRBs; `gitlab-agent-ocs-scanning-pod-sa` separately reads all secrets cluster-wide (~450 today; the count churns with runner ephemera). Scope the agent to per-project impersonation with an allowlist ClusterRole that **explicitly excludes write on `kyverno.io` and `admissionregistration.k8s.io`** — otherwise CI can delete the very policies that constitute the contract. Headlamp flips to OIDC user impersonation (the `admin`-group CRB already exists for humans). Sequenced in Phase 2; test on colony's project first.

---

## 7. Current → target mapping (all 54 live namespaces + pending)

Actions: **keep** (module adoption + contract labels — "keep" ≠ untouched), **adopt** (bring out-of-band ownership in), **external** (sibling-owned; governed at admission + label PR), **dissolve** (staged evacuation, then delete), **split**, **move**, **delete**. Every namespace gets contract labels in Phase 1 regardless.

| # | Namespace | Action | Tier | backup / exposure | Notes |
|---|---|---|---|---|---|
| 1 | aether-k8s-arch-labeler | **delete** (Phase 2, after Kyverno arch mutate soaks) | platform (interim) | none / none | in-house cluster-wide `:latest` pod mutator + own cert chain, replaced by a 30-line CEL mutate |
| 2 | aether-k8s-node-remediator | **adopt** | platform | none / none | ends the split-brain with node_remediation.tf |
| 3 | affine | keep | app | **critical** / internal | Dump and app config now target `affine-cnpg-rw` (fixed 2026-07-02); kill the plaintext DB password in pod env; STS retired 7d after verified restore |
| 4 | agent-sandbox-system | keep | platform | none / none | pin controller (v0.4.5 live vs v0.5.0 upstream; API v1beta1) |
| 5 | ceph-csi-cephfs | keep (name frozen) | platform (priv, justified) | none / none | align CSI 3.16.2 ↔ rbd 3.16.0 skew |
| 6 | cert-manager | keep | platform | none / none | explicit ns via module; fix step-issuer bound to `cert-manager/default` SA (Phase 0 sweep) |
| 7 | cilium-secrets | keep | platform | none / none | structural, exempt-listed |
| 8 | cnpg-system | keep | platform | none / none | already restricted; hosts the Barman-Cloud plugin |
| 9 | coder | keep, **relabel** | **agent** | standard / internal | arbitrary-code workspaces (148Gi, unbacked today) + standing creds; SA already ns-scoped; FQDN egress |
| 10 | colony-dev | **external** | guest | standard(colony-declared) / internal | sibling tofu; label PR; **flag to owner: rollout stuck ~62d, crashloop pod at 14,199 restarts**; postgres backup becomes colony's explicit declaration |
| 11 | colony-sandboxes-dev | **external** | sandbox (owner=colony) | none / none | generated **ns-wide** default-deny closes the label-conditional hole (today only pods labeled `colony.shdr.ch/sandbox-role` are constrained); colony's pinholes remain |
| 12 | crossplane-system | keep | platform | none / none | cloud+IdP creds = crown jewels; early netpol; **decide/sever the provider-TLS secrets syncing into vc-seven30** — platform key material crossing the tenant boundary |
| 13 | dawarich | keep | app | **critical** / internal | GPS PII; postgis→CNPG(postgis) when convenient |
| 14 | default | **evacuate workloads** | platform | none / — | delete the PSS-blocked globalping-probe dup (0/1 for 5d) Phase 0; gateway stays (never moves, §4.2); validate no-workloads-in-default |
| 15 | descheduler | **delete or resurrect** (owner picks, Phase 1) | platform (interim) | — | suspended cron, 0 pods = a paused decision, not a component; default recommendation: delete |
| 16 | deskplane | keep, **relabel** | **agent** | standard / internal | ns-secrets CRUD + cluster nodes-read RBAC = agent posture now, not a watch-note; chart pin ≠ image pin (`:latest`); **owner=aether** (hybrid channel: chart/image built by the deskplane repo's CI, but deployed by this repo's tofu — the deployer owns the namespace); registry-access=dockerhub override (kasmweb session images, §5.1) |
| 17 | games | keep | app (pss-override) | none, `data=rebuildable` (100Gi home: owner call) / **lan-vip** | steam-headless privileged + LB VIP 10.0.3.13 direct; LimitRange yes, quota no |
| 18 | gitlab-agent | keep, **de-god** | platform | none / none | cluster-admin → scoped per-project impersonation, no kyverno.io/admissionregistration write; OCS scanner loses cluster-wide secrets read |
| 19 | gitlab-runner | keep | platform (priv, justified) | none / none | deny-ingress + egress {gitlab, registry}; per-ns quota; aether-batch priority |
| 20 | globalping | keep, **reframe** | **guest** (pss-override: hostNetwork) | none / none | the internet's workloads on your LAN: egress=internet CNP (`toEntities: world`, deny RFC1918), no gateway, no cloned creds — the cheapest blast-radius win in the cluster |
| 21 | goldilocks | keep | platform | none / none | — |
| 22 | headlamp | keep, **de-god** | platform | none / none | SA cluster-admin → OIDC user impersonation |
| 23 | holyclaude | **decommissioned** | — | — | removed from tofu on 2026-07-05; namespace/workload teardown is owned by the matching IaC apply |
| 24 | hoppscotch | keep | app | standard / internal | STS retirement post-verified-restore |
| 25 | immich | keep | app | **critical** / internal | Dump and app config now target `immich-cnpg-rw` (fixed 2026-07-02); 2.1Ti NFS library already offsite via ZFS+restic |
| 26 | infra | **dissolve** (Phases 3–4) | **app (interim)** | — / internal (interim) | the catch-all namespace still mixes LLM/app/agent/platform concerns. **Interim contract labels in Phase 1 — tier=app, egress=internal, mesh=ambient (preserved at adoption, §5.2) — so the quarantine fence can never land on the namespace serving everything.** Splits: → litellm, openwebui, searxng, firecrawl, ai-serving, mnemo (app); → hermes, jupyter (agent); → ups (platform). Delete when empty; retire namespaces.tf |
| 27 | istio-system | keep (name frozen) | platform (priv, justified) | none / none | — |
| 28 | karakeep | keep | app | **standard** / internal | currently zero backup of any kind; prevent_destroy is not a backup |
| 29 | kepler | **merge → observability** (Phase 5) | platform | none / none | one privileged energy DS doesn't need its own ns; same scraper posture as node-exporter/otel |
| 30 | knative-operator | **delete** (owner-gated, Phase 0/1) | platform (interim if retained) | — | zero ksvc cluster-wide; teardown order per §9 (CRs → operator → ns) |
| 31 | knative-serving | **delete** (owner-gated) | platform (interim if retained) | — | 23 idle HA pods pinned to the amd64 relief valve; the largest pure-waste line item; reversal = one tf apply |
| 32 | kube-node-lease | keep | platform | — | structural; exempt-list stamp sets registry-access=none → loses cloned dockerhub secret (one-time cleanup, §5.3 row 9) |
| 33 | kube-public | keep | platform | — | structural; exempt-list stamp sets registry-access=none → loses cloned dockerhub secret (one-time cleanup, §5.3 row 9) |
| 34 | kube-system | keep | platform | — | Talos PSA-exempt; untouched **except gateway-access/exposure labeling — it hosts the hubble-ui HTTPRoute (hubble.home.shdr.ch → default/main-gateway), and Kyverno's default resourceFilters skip kube-system, so the stamper cannot label it: an explicit tofu-side `kubernetes_labels` resource carries it through the listener flip** (§9 Phase 2) |
| 35 | kyverno | keep | platform | none / none | the enforcer; 5 legacy policies → CEL in the same PR as the new contract |
| 36 | matrix | keep | app | **critical** / internal | Dump and app config now target `matrix-cnpg-rw` (fixed 2026-07-02); synapse media + bridge sessions (30Gi rbd, no file backup today) → snapshot schedule §8.8 |
| 37 | media | **split ×2, keep rest** (Phase 5) | app (pss-override until split → **baseline** after) | standard(configs) / **public until the split, then internal** | jellyfin (privileged FUSE sidecar + GPU) **and** qbittorrent (gluetun NET_ADMIN, qbittorrent.tf:136) both leave → remaining 10 apps drop to baseline. **jellyfin serves the public `tv.shdr.ch` listener from inside media until Phase 5 (jellyfin.tf:408, gateway.tf:93) — stamping media `internal` before the split would detach that route at the Phase 2 flip; `tv.shdr.ch` goes in media's hostnames annotation until then.** `local.jellyfin_ns` is shared by 13 files — split mechanics in Phase 5; ~108Gi configs join the backup set via the label |
| 38 | medik8s-leases | keep | platform | — | operator side-effect; exempt list |
| 39 | miniflux | keep | app | standard / internal | first restore-drill candidate (small, healthy CNPG) |
| 40 | nextcloud | keep (incl. onlyoffice) | app | **critical** / **public** | one stack, one lifecycle; CNPG cutover done in place 2026-07-02; legacy STS at 0 replicas, deletion gated by the 7-day rule |
| 41 | node-healthcheck-operator-system | keep | platform | none / none | Phase 0: delete leftover medik8s mutating webhook + 6 orphaned medik8s-system CRBs; name grandfathered at the 32-char cap (§4.1) |
| 42 | open-design | keep | app | standard / internal | — |
| 43 | osemu-ehis-farms | **external** | **guest** | **critical** / **tunnel** | client site, zero backup, plaintext sync token in CronJob spec, wordpress:latest at rev 20 with a live kubectl-patch, cloudflared bypassing gateway+Keycloak. Contract: exposure=tunnel legalizes and gates the tunnel; backup=critical drives a mysqldump path on scoped creds; token → Secret (Phase 0); **the orphan `osemuehisfarms.com` HTTPRoute (Accepted in-cluster, unwired publicly) is deleted at the Phase 2 hostname-validation flip — the tunnel is the declared ingress, and under exposure=tunnel the route would be a standing violation** |
| 44 | personal | **dissolve — fully** (Phases 3–4) | **app (interim)** | — / internal (interim) | the password vault shares secret scope with six mutable-tag apps (memos runs `:stable`, the rest `:latest`) and an orphaned PSS-blocked holyclaude deploy (delete Phase 0). Interim contract labels in Phase 1 (tier=app, egress=internal) so quarantine can never fence it mid-dissolution. Order: → vaultwarden (**first**), → composer (holds LLM provider keys — second), → orion, snapotter, vane, memos, bentopdf (small PVC batch, one scheduled week). Not "opportunistic" — at N=1 opportunistic means never. Delete when empty |
| 45 | policy-reporter | keep | platform | none / none | — |
| 46 | sandboxes | keep | sandbox | none / none | model citizen; its hand-written netpol/quota migrate into the Kyverno bundle (single-writer) |
| 47 | shdrch | **external** | guest | none / none | sibling tofu, barely labeled; guest digest route ends the 3-silent-cron-failures pattern |
| 48 | system | **narrow, keep name** (Phase 5) | platform (priv, justified) | none / none | name frozen by SC parameters + PV secretRefs. Sheds: otel, ksm, node-exporter → `observability`; nvidia device-plugin, dcgm, mps → `gpu-system`. Transitional ExternalName Service preserves `otel-daemonset…system.svc` DNS (mnemo.tf:75 literal + rendered configs; the Service itself is now tofu-pinned in otel_collector.tf) during the move. Keeps: rbd CSI + NFS CSI only |
| 49 | temporal | keep | app | standard / internal | replace `auto-setup:latest` in steady-state serving while touching it |
| 50 | tetragon | keep | platform (pss-override) | none / none | observe-only baseline stays; Hubble compensates for kata blindness |
| 51 | trivy-system | keep | platform (pss-override) | none / none | fix operator churn + failing scan jobs — a failing scanner trains you to ignore it |
| 52 | vc-seven30 | keep (name frozen) | **tenant** | host-side snapshots + tenant-declared / public (own listeners) | add first-ever quota; add **ingress CNP** (egress-only today); host-side scheduled VolumeSnapshots for the 163Gi (100Gi pg + a second vaultwarden, currently the largest unbacked pool in the cluster) — crash-consistent protection without host visibility; offer a scoped SeaweedFS identity for tenant-run CNPG backups; decide the crossplane TLS sync (row 12) |
| 53 | wasmcloud-system | keep | platform | none / none | retained per the wasm-as-IO-glue owner policy; flag: idle 2/2 hostgroup, 52-day Error pod — utilization review, deletion is the owner's call |
| 54 | yourspotify | keep | app | standard / internal | mongodump survives (non-CNPG), re-keyed to scoped per-ns S3 creds |
| — | **orion** (pending; currently a `personal` tenant via untracked orion.tf) | **move** to own ns in the personal dissolution | app | standard / internal | commit orion.tf Phase 0; resolve the `home.shdr.ch` three-way conflict (gateway `home-root` listener vs LAN Caddy 302→Grafana vs public 404 — the hostname is currently unreachable from every path) |

**New namespaces created by the program** (full contract per namespace — the module call is writable from this list without further decisions): `vaultwarden` (app / critical / internal), `litellm` (app / critical / internal — the AI-key vault), `openwebui` (app / critical / **internal**), `searxng` (app / none / internal), `firecrawl` (app / standard / internal, egress=internet), `ai-serving` (app / none `data=rebuildable` / internal), `observability` (platform / none / none), `gpu-system` (platform / none / none), `ups` (platform / none / internal), plus per-app namespaces created by the personal/infra dissolutions as they land.

---

## 8. Practices you did not know to ask for

*(Each specific to this cluster's live state, 2026-07-02.)*

1. **PSA rejections are silent, and it has already bitten you twice.** `enforce` evaluates Pods, not Deployments — the Deployment admits fine and pods just never appear. `default/globalping-probe` sat 0/1 for 5 days; `personal/holyclaude` for ~2.5 days. The applied 2026-07-02 checkpoint shipped the first guardrail: Grafana rule `k8s-replicaset-not-ready` alerts when `kube_replicaset_status_replicas − kube_replicaset_status_ready_replicas > 0 for 15m`. The sharper FailedCreate/PSA event join remains Phase 2 work; do not flip new PSA enforce gates until that signal, or an equivalent tested signal, is present.
2. **The hairpin defeats every control you're about to build.** affine, onlyoffice, orion, vane, and hermes call litellm/searxng via `https://*.home.shdr.ch` — traffic exits the cluster to the Caddy VM (your documented single-router 502 SPOF) and comes back as LAN ingress, invisible to netpol and mesh identity. Standardize in-cluster calls on cluster DNS **as part of each app's move checklist** — it's also why agent FQDN allowlists must account for LAN hostnames until each hairpin is converted.
3. **One Talos machine-config line makes kata namespaces restricted instead of privileged.** PSA exemption `runtimeClassNames: [kata]`: the VM boundary substitutes for the PSS controls, so sandboxes — and formerly holyclaude/mux before their 2026-07-05 decommission — carry `enforce=restricted` while kata pods keep their capabilities inside the guest.
4. **Your backup layer has a god-key, and ransomware knows this playbook.** The SeaweedFS **admin** keypair is cloned into 11 namespaces as `db-backup-s3` — and since 2026-07-02 it is also the `s3Credentials` for all 12 CNPG `barmanObjectStore` configs, so logical dumps, base backups, AND the WAL archive all hang off one admin key; any secret-reading compromise in any of those namespaces can delete every backup stream in the cluster. Per-ns scoped identities (module-provisioned) + bucket versioning + object-lock + lifecycle kill the god-key, the stale-target bug class, and unbounded dump growth in one mechanism — the CNPG adoption made this *more* urgent, not less.
5. **Hostname squatting is your top unpriced risk, and conflict resolution is "first-created wins."** All 8 listeners are `allowedRoutes: All` with 0 ReferenceGrants — any namespace can today claim `vaultwarden.home.shdr.ch` or attach to the public `nextcloud.shdr.ch` listener; Gateway API resolves conflicts by creation timestamp, so a route created during any gap wins silently. Three cheap layers: Selector attachment, pinned listener hostnames, hostname-⊆-allowlist validation. And the second ingress path is governed the same way: cloudflared images run only in `exposure=tunnel` namespaces.
6. **Make the mnemo bug an admission error and the affine/immich bug a policy.** "CNPG Cluster in a backup≠none namespace must declare backup" (Enforce) turns silent backup gaps into a rejected apply. "Legacy STS deleted within 7 days of verified cutover" closes the dual-DB window that produced two green-but-worthless backup streams. The adopted legacy STSes are now scaled down with PVCs retained; the rule is still needed for future cutovers and namespace moves.
7. **The CNPG cutover window is the cheapest namespace-move window you will ever get.** LiteLLM used the in-place cutover path on 2026-07-02 and is now CNPG-backed in `infra`. Future database namespace moves must keep the app, writer service, and backup target aligned in the same change. OpenWebUI is already cut over to CNPG in `infra`; its future namespace move has the same constraint.
8. **Ceph-RBD file data has no file-level backup at all — zero VolumeSnapshot CRDs exist.** Install external-snapshotter + a VolumeSnapshotClass; drive nightly snapshots from the backup label for the small high-sensitivity RWO volumes (vaultwarden 5Gi, synapse media + bridge sessions 30Gi, karakeep, dawarich, hermes workspaces) **and vc-seven30's 163Gi** (crash-consistent tenant protection without host visibility). **Exclude CNPG instance PVCs** — snapshot-without-fencing is unsafe; Postgres restores only via `bootstrap.recovery`.
9. **prevent_destroy is better than it was, but still hand-plumbed.** The 07-02 CNPG adoption grew the set from 18 blocks/11 files to **33 blocks/19 files** and closed the worst gap — all 12 CNPG clusters are now protected. Several new blocks deliberately guard the *legacy* rollback PVCs (litellm, immich, affine…) — correct during the 7-day retirement windows, garbage-guarding after; strip each with its STS. The structural fix stands: re-key protection to `backup=critical` via the module so it derives from the contract instead of accreting by hand.
10. **Orphaned RBAC is privilege resurrection, not clutter.** Nine CRBs still bind SAs in deleted namespaces (`medik8s-system` ×6 — node-healthcheck ×3 + self-node-remediation ×3, `olm`, `kubero-operator-system` ×2) plus a leftover medik8s mutating webhook still intercepting API calls, plus step-issuer bound to `cert-manager/default`. Anyone who recreates those ns+SA names silently inherits the grants. Sweep in Phase 0, then keep a **standing Audit-mode policy flagging ClusterRoleBindings whose subjects reference nonexistent namespaces** — the class, not the instances.
11. **Digest-pin exactly where it pays.** Half the cluster runs `:latest`; a cluster-wide tag gate is instant self-DoS. Scope an ImageValidatingPolicy (digest-or-immutable-tag) to `backup=critical` namespaces only — temporal serves from `auto-setup:latest`, matrix runs `:latest` in all four containers, osemu runs `wordpress:latest` at revision 20. Everything else gets a warn-only `:latest` report.
12. **One kube-state-metrics flag makes the whole scheme queryable — otherwise it's invisible.** `--metric-labels-allowlist=namespaces=[aether.shdr.ch/tier,…owner,…backup,…criticality]` turns contract labels into PromQL join keys: tier-routed alert severity (~4 routes), backup-freshness-by-contract, Kepler watts-per-tier showback on fixed hardware. Add the same keys to OTel `k8sattributes` once; traces and logs carry them forever.
13. **Restore order is derived, not remembered — and your migration IS the drill program.** `task dr:plan` renders live label state into an ordered rebuild checklist (an etcd snapshot restores the control plane, *not* your applications). Because every move in Phase 4 executes as `bootstrap.recovery` or snapshot-restore into the new namespace, by the end of the program you will have proven restore on every critical datastore without scheduling one artificial drill. Quarterly thereafter: one random critical-ns restore + one CNPG PITR.
14. **The delete-guard is two hours of work and one averted catastrophe.** Namespace DELETE on persistent+critical namespaces requires an explicit same-change annotation. `kubectl delete ns vaultwarden` at 1am should be a conversation, not an outcome.
15. **VAP is the answer to "who guards the guard."** Kyverno's resource webhook is fail-closed — good — but that makes Kyverno an availability dependency and a rebuild-ordering problem. The two VAP invariants run in-process in the apiserver: they hold during a Kyverno outage and during rebuild waves before Kyverno exists. Twenty lines of CEL removes the single point of policy failure.
16. **ESO + your existing OpenBao is the secrets end-state — after boundaries.** You already run Bao with per-project transit engines (the osemu pattern). external-secrets-operator with **per-namespace SecretStores** bound to per-namespace Bao policies makes secret provenance namespace-scoped and auditable; disable ClusterSecretStore outright. **Checkpoint 2026-07-04:** ESO v2.7.0 is installed, every namespace has a Ready `openbao` SecretStore, no ClusterSecretStore exists, and the Kyverno Docker Hub clone-source secret is ESO-managed from `kv/aether/kubernetes/kyverno/dockerhub-creds`. Continue retiring direct Terraform-managed app secrets only when each consumer can tolerate an ESO refresh/ownership handoff.

---

## 9. Migration plan

Every phase leaves the cluster strictly better and is a safe stopping point. Risk: 🟢 reversible · 🟡 reversible with effort · 🔴 one-way, gated on verification + owner confirmation.

**Decommission ordering rule (applies to every deletion in every phase):** teardown is CRs first → operator second → namespace last, encoded as tofu `depends_on` from app releases to operator releases so destroy order inverts correctly; verify the namespace empties before deleting it; never force-remove finalizers without diagnosing what holds them.

**Phase 0 — stop the bleeding (this week; no design dependencies).**
**Step ZERO, before any broad `tofu apply`: completed 2026-07-02.** The six live-only CNPG clusters/secrets (affine, immich, litellm, openwebui, matrix, nextcloud) are now declared in tofu, and the affected app/database backup configs render the CNPG writer services instead of the frozen legacy STSes. This removed the full-apply rollback class that would have sent apps or green backup jobs back to stale databases.
Then: 🟢 Verified first post-retarget dumps for affine, immich, openwebui, matrix, nextcloud, and litellm. 🟢 Native CNPG object-store backups are configured for all 12 current clusters; a 2026-07-02 manual sweep completed and verified SeaweedFS objects. 🟢 Commit the remaining working-tree drift and add the nightly tofu-plan drift CI job. 🟢 Delete the two PSS-blocked orphan deployments (`default/globalping-probe`, `personal/holyclaude`) + their Released PVs. 🟢 Decommission mux + holyclaude from repo-owned IaC on 2026-07-05. 🟡 Sweep dead-install residue: 9 orphaned CRBs (medik8s-system ×6, olm, kubero ×2), the medik8s webhook, the step-issuer default-SA binding; triage the 11 Released PVs (~119Gi — each deletion listed and owner-confirmed). 🟢 SeaweedFS bucket: versioning + object-lock + lifecycle. 🟢 osemu sync token → Secret; 🟡 delete the orphan osemu HTTPRoute at hostname-validation flip. 🟡 Confirm descheduler/wasmcloud owner calls. 🟢 LiteLLM/OpenWebUI/Matrix CNPG retargets complete; keep the old STSes scaled down + PVC-retained only through their rollback window, then delete STS+PVCs by targeted tofu changes.
*Rollback:* dump-target changes revert in one commit; deletions are of provably broken/orphaned objects; step zero is itself pure reconciliation — it changes tf to match live, never live to match tf.

**Phase 1 — the label layer (weeks 1–2). Risk 🟢: labels + Audit only, zero workload impact — partially applied 2026-07-02; remaining items listed below.**
Build `modules/namespace`; adopt the 33 tf-owned namespaces via `moved` blocks and the 7 helm-created ones via `import` blocks (§5.2); gate every adoption on the **label-preservation diff script** — "plan shows labels-only" is NOT sufficient, because a label *removal* (infra's ambient label or goldilocks flags) is labels-only and silently breaks mesh/telemetry; flip the 7 helm releases to `create_namespace=false`. Every namespace gets contract labels, including interim labels on the dissolving pools (infra/personal: tier=app, §7 rows 26/44). Deploy the Kyverno CEL stamper + require-labels validate in **Audit**; the quarantine *mutate + report* goes live now, but its *generate* remains disabled until the namespace contract has soaked.

**Applied checkpoint 2026-07-02:** the namespace module/adoption layer is live; the 7 Helm-owned namespaces are imported and their releases use `create_namespace=false`; all 54 live namespaces have the four contract labels; `namespace-contract-labels` is deployed as Kyverno CEL `ValidatingPolicy` in `Audit` with `failurePolicy: Ignore`; nightly scheduled CI drift planning exists; and Grafana rule `k8s-replicaset-not-ready` is provisioned. **Still remaining in Phase 1+:** Kyverno mutate-stamper/quarantine reporting and generated quarantine resources, label-derived PSA/Gateway/Goldilocks stamping, sibling-repo label PRs, PolicyReport soak, and any Enforce flip.
*Rollback:* remove policies; labels are inert.

**Phase 2 — enforcement ratchet (weeks 2–6, per-step independent). Risk 🟡 per step.**
Label-gate the dockerhub clone: pre-label legitimate consumers **including the agent-tier overrides (hermes, deskplane — §5.1; they pull Docker Hub images today and would hit anonymous rate limits on next reschedule)**, port the SA/Pod imagePullSecrets mutate rules in the same change, then flip the gate, then run the one-time orphan-secret cleanup (§5.3 row 9). PriorityClasses ×4 + legality validate + pod mutate, sequenced per §5.3 row 10 (classes → soak → rolling restarts; lowest class is the globalDefault). LimitRanges on all non-platform tiers; quotas (sandbox, tenant, guest, gitlab-runner). CCNP baseline (DNS + apiserver) first, then netpol per-tier with Hubble observation → per-ns deny flips — **the app-tier ingress baseline is a CNP with `fromEntities: [ingress]` because Gateway API traffic arrives from Cilium's ingress identity, not from a namespace selector**. Hostname allowlist validation + Gateway listener `allowedRoutes: Selector`; cloudflared gate for `exposure=tunnel`; CNPG backup rule starts Audit.
*Rollback:* each flip is one label/listener/policy revert; PSS alert catches silent rejections.

**Phase 3 — the backup contract (weeks 4–8, overlaps Phase 2). Risk 🟡; STS deletions 🔴 gated.**
Barman-Cloud plugin in cnpg-system; per-ns SeaweedFS identities + ObjectStores + ScheduledBackups; CNPG-backup rule Audit→**Enforce**. Interim native CNPG backups are already configured and tested for all 12 current clusters via in-tree `barmanObjectStore` and shared SeaweedFS credentials, so the remaining work is identity/plugin hardening plus restore drills. external-snapshotter + VolumeSnapshotClass + label-driven schedules (incl. vc-seven30's 163Gi; CNPG PVCs excluded). First restore drill: miniflux. Then **vaultwarden moves** — the first namespace move, executed as snapshot-restore, highest sensitivity-per-GB, currently zero backup. LiteLLM, OpenWebUI, Matrix, and Nextcloud are already using their CNPG writer services in place. Legacy STSes are scaled to zero with PVCs retained; deletion remains 🔴 gated until after a verified restore and soak. **Cutover-into-new-namespace rule: db_backups.tf hardcodes several targets to their current namespaces, and future moved dump jobs can keep green-dumping the frozen database left behind, the exact affine/immich failure mode (§8.6). The same commit that cuts a database over into a new namespace must retarget its db_backups.tf entry to the new namespace/cnpg-rw service or delete it outright.** Retire db_backups.tf postgres jobs as Barman verifies; mongo/mysql/postgis dumps re-keyed to scoped creds. prevent_destroy re-keyed to the backup label. Digest-pin policy on backup=critical (Audit→Enforce).
*Rollback:* with all four in-place cutovers landed (07-02), the legacy STSes are frozen rollback artifacts, not live write targets — re-pointing a dump job at one is never a fallback; it green-dumps a frozen database. Rollback for a cutover is scale-up-the-legacy-STS + repoint the app (accepting loss of writes since cutover) — which is exactly why the 7-day retirement window exists. For future moves into new namespaces the fallback is the verified pre-move backup + the retargeted job.

**Phase 4 — topology moves (months 2–3; one move per week, each independent). Risk 🟡 per move; old-ns deletion 🔴 with PVs Retained ≥30 days as the undo.**
**Mandatory pre-move grep gate, every move, no exceptions:** `grep -rn '<ns-local>\|\.infra\.svc' *.tf *.tftpl` for every service leaving the namespace — plus the rule that caught what hand-enumeration missed: **any URL built from the MOVING app's OWN namespace local that actually addresses a service owned by ANOTHER app must be re-keyed to the target service's namespace local before the move** (these render cleanly and break silently). Known instances, verified in code (re-verified 2026-07-02 evening): mnemo.tf:55 (litellm URL keyed to mnemo's ns → re-key to litellm's ns local), hermes.tf:16 (llama-swap keyed to hermes' ns → re-key to ai-serving's ns local, or hermes' local model backend dies when hermes moves before ai-serving), firecrawl.tf:485 (searxng URL keyed to firecrawl's ns → re-key to `local.searxng_ns`). A fourth instance — mnemo's openwebui-postgres URL — was **resolved 07-02**: re-keyed to openwebui's ns local *and* repointed to `openwebui-cnpg-rw`, which also retires the Phase 3 mnemo-repoint step.
Order: kata RuntimeClass ownership → platform file (completed before the 2026-07-05 mux/holyclaude decommission) → hermes + mnemo out of infra (**neither is coupling-free**: mnemo carries the two own-ns-keyed URLs above and hermes carries the llama-swap one — "days old" ≠ zero calcification; hermes gets the first toFQDNs allowlist — its endpoints are already enumerated in hermes.tf) → jupyter out (keeps nvidia runtime) → litellm (already CNPG-backed; flip the true `litellm.infra` literal nextcloud.tf:72 in the same commit — affine.tf:418 is NOT a literal, it interpolates `local.litellm_ns` (litellm.tf:16) and auto-propagates when that local is re-keyed, needing only the re-render/restart; **the litellm tftpl backend lines are NOT flipped here** — litellm_config.yaml.tftpl services move with their own apps) → openwebui/searxng/firecrawl/ai-serving, each with its own restore/restart gate.

**Phase 5 — committed splits + cleanups (months 3–4).**
media: jellyfin AND qbittorrent out → media flips to baseline for the remaining 10 apps and drops its exposure from public to internal (tv.shdr.ch leaves with jellyfin, §7 row 37). **Split mechanics — the shared local is the trap:** `local.jellyfin_ns` (jellyfin.tf, = media) is referenced by 13 files, but only jellyfin and qbittorrent move — pointing the shared local at the new namespace would render hermes' sonarr/radarr/lidarr/prowlarr/sabnzbd/qbittorrent URLs (hermes.tf:151-156) and jellyfin's nzbdav rclone config (jellyfin.tf:359) into namespaces where those services do not live, silently. So: **first a no-op refactor commit introducing separate `media_ns`, `jellyfin_ns`, and `qbittorrent_ns` locals and re-keying all 13 files to the correct one (hermes.tf:151-156 → media_ns except QBITTORRENT_URL → qbittorrent_ns; jellyfin.tf:359 → media_ns, where nzbdav stays) — plan must show no changes — then the moves** (config PVC rebinds; cache re-warm acceptable). system narrowing: `observability` (otel, ksm, node-exporter; kepler merged) + `gpu-system` (nvidia, dcgm, mps) — stateless moves + the transitional ExternalName for the otel DNS + **the generated scraper allow switches from `system` to `observability` in the same commit** (§5.3 row 2). Owner decisions executed: descheduler, wasmcloud utilization review. Steady state begins: quarterly = one random critical restore drill + one CNPG PITR + attrition sweep (live vs module + external registry) + Goldilocks→LimitRange tuning.

**Phase 6 — secrets end-state (optional, quarter+).** ESO + OpenBao per-namespace SecretStores are live as of 2026-07-04; ClusterSecretStore is absent; the first cloned secret (`kyverno/dockerhub-creds`) is ESO-managed and still feeds the label-gated Kyverno generator. Remaining work is deliberate per-consumer retirement of direct Terraform-managed app secrets; do not bulk-migrate generated DB/bootstrap secrets without a restart and rollback plan for that workload.

---

## 10. Explicitly rejected alternatives

Recorded so future sessions argue against a written verdict instead of re-litigating.

| Rejected | Why |
|---|---|
| **Renaming `system`/`infra`/`media`/pool prefixes (`pool-*`, `app-*`, `sbx-*`)** | Pure cost (§4.2); prefixes encode tier in names — the label carries it. Diverges from the 06-21 doc, which *proposed* forward prefixes: rejected even for new namespaces. |
| **Moving `main-gateway` out of `default` / a gw-internal+gw-public edge split** | Three-repo parentRef coordination + VIP churn for near-zero risk reduction once Selector + pinned hostnames + hostname validation land. Governance without a move. |
| **A `ci` tier; `structural`/`operator` sub-tiers of platform** | Tiers of one member are taxonomy for its own sake; per-ns PSA + a quota override cover gitlab-runner. |
| **Merging agent and sandbox tiers** | Loses the credential-holding distinction — sandboxes must never hold standing creds. |
| **jupyter or any GPU workload in the kata-mandated sandbox tier** | kata and nvidia runtimeClasses are mutually exclusive; the placement breaks GPU access. GPU + arbitrary code = agent tier. |
| **Splitting only jellyfin out of media** | Falsified: qbittorrent's gluetun needs NET_ADMIN (qbittorrent.tf:135-137), forbidden at PSS baseline. Both leave or media stays privileged — two proposals carried this error; the plan does not. |
| **Freezing `personal` as a permanent pool** | Diverges from the platform-eng spine deliberately: composer's LLM provider keys cohabiting with `:latest` trinkets violates the secret-scope principle, and "opportunistic" at N=1 means never. Full dissolution, scheduled. |
| **Hand-set `restore-wave` label (required on every ns)** | Duplicates the tofu DAG, exercised only during a rebuild, rots exactly when it matters. Derive order from tier; generate the runbook. |
| **Granting the guest tier pull-secrets by default / quarantining strangers as `guest`** | An unattributed namespace must never inherit credentials. Unclassified gets default-deny + nothing. |
| **A `data-class` label alongside `tier`+`criticality`** | Two labels feeding the same alert routing = a mis-pick trap. |
| **HNC / Capsule / kiosk** | Archived or org-scale; flat + labels + vcluster won. vcluster only for real tenants — never for own apps. |
| **Blanket ResourceQuota; per-ns Loki tenants; per-app dashboards; per-ns human RBAC; waypoint L7 authz** | Org-cosplay at N=1. One honest cluster-admin human; the RBAC that pays is automation principals. |
| **Image signing / registry allowlists / cluster-wide `:latest` hard gate** | Org-scale for one operator pulling ghcr/lscr/docker.io daily; a hard tag gate at ~50% `:latest` prevalence is self-DoS. Digest-pin scoped to backup=critical; warn-only elsewhere. |
| **Deriving mesh enrollment from tier today** | Ambient covers 2/54 namespaces; deriving it would pretend an enforcement layer exists. Label-driven opt-in until rollout resumes. |
| **Keeping the arch-labeler webhook ("not urgent")** | An in-house cluster-wide `:latest` pod mutator with its own cert chain, doing what a 30-line Kyverno mutate does. Retired in Phase 2. |
| **Merging ceph-csi-cephfs into system** | Touches SC secret refs + the games static PV for cosmetics. Only if the CSI charts are being reworked anyway. |
| **Weekly restore drills; a second physical "staging" cluster; env-suffixed namespaces** | Quarterly single cadence; one environment exists; vcluster covers experimentation. |
| **Required `app.kubernetes.io/part-of` + `managed-by` on namespaces** | tier + owner already feed every live consumer (alerts, showback, provenance); the `source` annotation covers repo attribution. Revisit part-of only if grouping dashboards materialize. |
| **Velero** | Displaced, not overlooked: tofu rebuilds the manifests; CNPG Barman + CSI snapshots cover state; a second cluster-wide backup controller would be a second god-credential surface. |
| **A `registry-gitlab` value / GitLab pull-secret label** | GitLab deploy tokens are already per-namespace and per-app with no clone mechanism to gate; `registry-access` gates the one credential that is actually cloned (dockerhub). |
| **Enforced one-tf-file-per-namespace isomorphism** | Adopted aspirationally via the module + `source` annotation (Appendix B.6 shows the live cross-file couplings); enforcing it repo-wide is a refactor with no runtime payoff — the annotation makes ownership queryable, which is the part that pays. |

**Divergences from the 2026-06-21 doc, summarized:** its zero-renames conclusion is *kept* but re-derived from arithmetic instead of conservatism — and it no longer implies zero *moves*: infra, personal, media, and system all change shape via paid-for migrations. Its relabel-in-place-only stance is discarded. Its census is superseded (54 not 55; colony-* were missed; diun/renovate are **gone** — decommissioned out-of-band with zero repo record, and must not be referenced as live; 7 namespaces are new since 06-21, plus the 2 census-missed colony namespaces; its "no PSA floor" diagnosis was wrong — Talos enforces a cluster-default `enforce=baseline`, proven by live rejections; its prevent_destroy count undercounted — 18 blocks across 11 files at inspection, 33/19 after the 07-02 CNPG adoption). Its forward-prefix naming is rejected. Its hardcoded-literal inventory overcounted interpolated locals as literals (corrected in Appendix B).

**Graft ledger:** every graft mandated by the three judge panels is incorporated (media two-app split; 54-count correction; globalping-as-guest; CNPG-cutover-window ordering; vc-seven30 ingress CNP + crossplane-TLS decision; hairpin conversion per move; three-layer gateway + tunnel gating; standing CRB audit; ESO/OpenBao end-state; delete-guard; exclusion-lists→selectors; transitional ExternalName; VAP rebuild rationale + derived restore ordering; scoped digest-pin; rejected-alternatives appendix; PSS-blocked alert as Phase-1 prerequisite; unclassified-gets-nothing quarantine; external-snapshotter with CNPG exclusion; dual-DB 7-day rule; CI ClusterRole excluding policy-API writes; prevent_destroy re-key; host-side tenant snapshots; moved-block adoption; one-screen test; deliberately-not-enforced list; egress `internal` value; coder+deskplane→agent). None were rejected.

---

## Appendix A — current-state census (pre-checkpoint inspection 2026-07-02, with post-checkpoint deltas)

**2026-07-02 census:** 54 live namespaces before later decommissions. **2026-07-05 IaC delta:** mux and holyclaude are removed from repo-owned Terraform/Ansible and will be torn down by the matching apply.

Key facts below are the 2026-07-02 pre-checkpoint inspection unless a post-checkpoint delta is called out. **Applied checkpoint delta:** all 54 live namespaces carry the four `aether.shdr.ch/*` contract labels; the seven former Helm-created namespaces are imported into tofu and their Helm releases use `create_namespace=false`; `namespace-contract-labels` is live in Audit/Ignore; nightly scheduled drift planning exists; Grafana ReplicaSet-not-ready alert is provisioned; and ESO/OpenBao per-namespace SecretStores are live with no ClusterSecretStore. **2026-07-05 delta:** mux and holyclaude are decommissioned in IaC.

- **PSA:** Talos cluster-default `enforce=baseline` confirmed empirically (rejections in unlabeled `default` and `personal`). Pre-2026-07-05 per-ns counts included holyclaude; post-decommission counts require a fresh live census after apply.
- **Network:** flat for 50/54 namespaces. Policies exist only in sandboxes (true default-deny), colony-sandboxes-dev (label-conditional — unlabeled pods unconstrained), colony-dev (one ingress scope), vc-seven30 (2 egress CNPs, **no ingress policy**). 0 CiliumClusterwideNetworkPolicies.
- **Quota/LimitRange:** exactly one namespace (sandboxes). Everything else unbounded; `infra/ups-management` was OutOfmemory-rejected on a saturated Pi.
- **Routing:** 1 Gateway (`default/main-gateway`, VIP 10.0.3.19), 8 listeners all `allowedRoutes: All`, 66 HTTPRoutes across 25 namespaces (61 in-repo parentRefs + colony's 4 + the vcluster route), **0 ReferenceGrants**. TLS is 100% external (Caddy VMs). LB pool: 10.0.3.19 (gateway), 10.0.3.13 (games), 10.0.3.21 (vcluster API). Second ingress path: osemu's Cloudflare Tunnel, bypassing gateway + Keycloak.
- **Data:** 104 PVs (93 Bound, 11 Released ~119Gi awaiting triage), all Retain via Kyverno mutation. ~8.5Ti requested; immich 2.1Ti + nextcloud 2Ti + media 1Ti on NFS (offsite-covered via ZFS+restic); ~2.3Ti ceph-rbd with **no file-level backup mechanism — zero VolumeSnapshot CRDs, no Velero**. 12 CNPG clusters across 9 namespaces are now declared in tofu and have native CNPG object-store backup configured and manually verified. Cut over: affine, coder, hoppscotch, immich, firecrawl, litellm, miniflux, temporal, mnemo, openwebui, matrix, nextcloud. 13 db-backup CronJobs; affine, immich, openwebui, matrix, nextcloud, and litellm now target the CNPG writer services. SeaweedFS admin keypair cloned into 11 namespaces; bucket has no versioning/lifecycle.
- **Secrets:** ~450 total (444 at inspection; churns with runner ephemera). Post-2026-07-04 delta: ESO v2.7.0 runs in `external-secrets`; every namespace has a Ready `openbao` SecretStore bound to a namespace-scoped OpenBao Kubernetes-auth role; no ClusterSecretStore exists; `kyverno/dockerhub-creds` is now an ESO-managed `ExternalSecret` sourced from `kv/aether/kubernetes/kyverno/dockerhub-creds`, and Kyverno still generates `dockerhub-creds` into Docker Hub allowlisted namespaces. Crown jewels still needing per-consumer migration decisions: crossplane AWS+Keycloak creds (TLS material synced into vc-seven30), CSI Ceph keys, PKI roots, litellm provider keys, app registry tokens, and generated DB/bootstrap credentials.
- **RBAC:** cluster-admin CRBs on `gitlab-agent/gitlab-agent` and `headlamp/headlamp`; `gitlab-agent-ocs-scanning-pod-sa` reads all secrets cluster-wide; hermes-tungsten cluster-wide read (no secrets, but pod logs); deskplane ns-secrets CRUD + cluster nodes read; 9 orphaned CRBs (medik8s-system ×6, olm, kubero ×2) + step-issuer→`cert-manager/default` ×2 (manager + proxy) + a leftover medik8s mutating webhook.
- **Policy engines:** Kyverno 1.17.2 with 5 legacy ClusterPolicies (2 Enforce: arm-pool-guardrails, cnpg-require-ceph-rbd-storage; 3 Audit/mutate), fail-closed resource webhook. Tetragon: 6 observe-only TracingPolicies; blind to kata pods. Istio ambient: 2 namespaces only (infra, osemu). Goldilocks: 18 namespaces.
- **IaC:** Pre-checkpoint: 88 tf files (+5 tftpl; cnpg_adopted.tf + cnpg_backups.tf added 07-02); 33 `kubernetes_namespace_v1` + 2 kubectl_manifest namespaces; 7 helm `create_namespace=true` (label-less); all 07-02 working-tree drift is now committed (orion.tf, db_backups.tf mid-program, then the CNPG reconciliation set); mazanoke.tf deleted; 33 prevent_destroy blocks in 19 files (all 12 CNPG clusters protected as of 07-02). Post-checkpoint: namespace ownership is centralized through `modules/namespace`, the former Helm-created namespaces are imported, affected Helm releases use `create_namespace=false`, and mux/holyclaude are removed from repo-owned IaC on 2026-07-05.
- **Waste/health:** knative 23 idle pods / 0 ksvc; descheduler suspended, 0 pods; wasmcloud idle 2/2 + 52d Error pod; colony-api rollout stuck ~62d (crashloop pod 14,199 restarts); trivy operator churn + failing scans; 2 PSS-blocked orphan deploys (default/globalping-probe 5d, personal/holyclaude ~2.5d); shdrch cron failed 3 of last 4 runs unnoticed; agent-sandbox v0.4.5 vs v0.5.0 upstream; CSI skew 3.16.2/3.16.0.
- **Out-of-band delta vs 06-21:** missed then: colony-dev (b. 04-25), colony-sandboxes-dev (b. 04-29), kube-public, kube-node-lease. New since: open-design, cnpg-system, globalping, games, osemu-ehis-farms, deskplane (+ vane/snapotter/orion/mnemo as tenants of existing ns). Removed/decommissioned since, with repo record now present for mux/holyclaude: **diun, renovate, mux, holyclaude**.

## Appendix B — load-bearing couplings (verified against current code)

1. **StorageClass parameters are immutable and embed CSI secret namespaces**; all ~90 bound ceph PVs carry provisioner/nodeStage secretRefs into `system` / `ceph-csi-cephfs` → those names are frozen forever.
2. **Cross-namespace service references — three classes, each with different move mechanics.** (a) *True hardcoded literals* (hand-edit at move): `litellm.infra.svc` (nextcloud.tf:72), `searxng.infra.svc` (vane.tf:16), `llama-swap/speaches/firecrawl.infra.svc` (litellm_config.yaml.tftpl:226/240/287 — flipped at the ai-serving and firecrawl moves, NOT the litellm move), `otel-daemonset….system.svc` (mnemo.tf:75), `dawarich-postgres.dawarich.svc` (dawarich.tf:64), `cilium-gateway-main-gateway.default.svc` (main.tf:241 → knative.tf), `istiod.istio-system` (otel_collector.tf:278), `cert-manager-istio-csr.cert-manager.svc` (cert_manager.tf:36). (b) *Interpolations of the TARGET's own ns local* (auto-propagating — re-key the local, re-render, restart; never hand-edit): **affine.tf:418** (`litellm.${local.litellm_ns}` — the 06-21 doc's overcounting class, corrected here too), jellyfin.tf:359, aiostreams.tf:118, hermes.tf:151-156 media URLs, hermes.tf:19 (`local.searxng_ns`). (c) ***Interpolations of the CONSUMER's own ns local addressing another app's service*** — the dangerous class: renders cleanly, breaks silently at the consumer's move; must be re-keyed to the target's ns local *before* the move: **mnemo.tf:55** (litellm), **hermes.tf:16** (llama-swap), **firecrawl.tf:485** (searxng). (mnemo's openwebui-postgres URL was in this class until 07-02, when it was re-keyed to openwebui's ns local and repointed to CNPG — one down.) Phase 4's grep gate exists because class (c) is invisible to a literal-hunt.
3. **S3 key prefixes embed namespace names** (`db_backups.tf`: `postgres/<ns>/<name>`, `mongo/<ns>/<name>`) — a move forks dump history. The CNPG barman path added 07-02 already keys to cluster name (`s3://aether-db-dumps/cnpg/<cluster>`), so this concern now applies **only to the legacy dump CronJobs** — CNPG-backed apps can move namespaces without forking backup history, which makes the Phase 4 moves cheaper than originally priced.
4. **Gateway coupling:** 61 in-repo parentRefs to `{main-gateway, default}` + colony repo (4 routes) + vcluster syncer route; VIP 10.0.3.19 baked into home + VPS Caddyfiles and AdGuard. Any hostname change touches **five surfaces**: gateway listeners (tofu), home Caddyfile (ansible), VPS Caddyfile (ansible), Cloudflare DNS (tofu), AdGuard rewrites (nix).
5. **vc-seven30:** syncer-encoded service names (`caddy-ingress-x-default-x-seven30`), OTel rewrite regex `^vc-seven30($|-)` (2 places), tenant API VIP 10.0.3.21, listener sectionNames seven30/seven30-root. Note: `istio.tf:6`'s comment claiming vc-seven30 is ambient-labeled is **false** in code and live.
6. **Cross-file ownership:** jellyfin.tf owns `media` (12 dependent files via `local.jellyfin_ns` — 13 files reference it counting jellyfin.tf itself); ceph_csi.tf owns `system` (6+ dependents + outputs); personal_apps.tf owns `personal` (7); namespaces.tf owns `infra` (14+); onlyoffice.tf deploys into nextcloud's ns; game_server.tf reads a secret from ceph-csi-cephfs (static CephFS PV); node_remediation.tf wires RBAC into the sibling-owned `aether-k8s-node-remediator` ns (node_remediation.tf:27); db_backups.tf reaches into 10 files' locals + hardcodes 13 target namespaces; openwebui.tf:7 seeds its ns as a bare string.
7. **prevent_destroy (33 blocks / 19 files as of 07-02):** the original 18 (affine ×2, dawarich, firecrawl ×3 incl. its CNPG Cluster, hoppscotch, karakeep ×2, matrix ×4, miniflux, open-design, snapotter, vaultwarden, yourspotify) plus the CNPG-adoption set: the 6 adopted clusters (cnpg_adopted.tf ×6), mnemo's cluster, the coder/hoppscotch/miniflux/temporal/nextcloud/openwebui cluster declarations, and the litellm/immich legacy rollback PVCs — any move of these trips plan-time errors; lifted by `tofu state mv` + lifecycle edit in the same commit as the move, never by force.
8. **Exclusion lists that rot:** 3 Kyverno policies (`kube-system, kube-public, kube-node-lease, istio-system, system, kyverno` ± arch-labeler) + the arch-labeler webhook namespaceSelector — all replaced by tier selectors in the CEL port.
9. **Hairpin consumers** (in-cluster → `*.home.shdr.ch` → Caddy VM → back): affine.tf:94, onlyoffice.tf:16, orion.tf:77, vane.tf:17, hermes.tf:15/18/20-22 — converted to cluster DNS per move (§8.2).
10. **Live-vs-repo drift to reconcile:** `s3.seaweed.home.shdr.ch` has no repo Caddyfile vhost yet 11 namespaces consume it (13 db-backup CronJobs + all 12 CNPG barman configs; live Caddy drifted ahead — the seaweedfs LXC playbook was committed 07-02, but the Caddy vhost is still live-only); `home.shdr.ch` three-way conflict (gateway home-root listener vs LAN Caddy 302 vs public 404); `osemuehisfarms.com` route Accepted in-cluster but unwired publicly.
